# 正文第13章：三主轴不等刚体不受力时的自由运动

*Concerning the Free Motion of the Said Bodies with Three Unequal Principal Axes, Acted on by No Forces*

---

## 本章概述

欧拉在本章中讨论了最一般、也最困难的自由旋转情形——三个主转动惯量彼此不等的刚体在无外力作用下的运动。与前两章的球对称体和旋转对称体不同，这类刚体的自由旋转无法用初等函数表示——其解最终归结为椭圆积分。欧拉推导了描述转动轴和角速度随时间变化的基本方程，虽然他未能完全求解，但他建立的分析框架后来成为处理一般自由旋转问题的基础。

> **批注**：三轴不等刚体的自由旋转是刚体动力学中第一个真正困难的问题。欧拉在这里面对的是一个非线性耦合的微分方程组，无法用当时的数学工具（代数函数、三角函数、对数函数）来积分。这推动了对椭圆函数的研究——后来由勒让德、阿贝尔和雅可比在19世纪完成。从现代观点看，三轴不等体的自由旋转可以用雅可比椭圆函数精确表达，角速度的端点在惯量椭球上描绘出所谓的"本体极迹"（polhode），这是一个封闭曲线。

---

## 问题76

**737.** 如果初始给某个刚体施加某种转动运动，且该刚体不受任何外力作用，求转动轴在任意时刻相对于主轴的位置。

### 解

刚体的质心 I 保持静止，以它为中心建立一个球面（图89）。设 IA, IB, IC 为刚体的主轴，关于轴 IA 的转动惯量 = Maa，关于轴 IB = Mbb，关于轴 IC = Mcc——假设它们彼此不等（因为如果两个甚至三个相等，就回到前面章节的情况了）。

经过时间 t，设直线 IO 为转动轴（axis of rotation），刚体以角速度 $\alpha'$ 绕之在 ABC 方向上转动。需要求出转动轴相对于主轴的位置。设大圆弧 OA = $\zeta$，OB = $\eta$，OC = $\theta$，它们是随时间变化的变量，满足：

$$\cos^2\zeta + \cos^2\eta + \cos^2\theta = 1$$

**推理过程**：转动轴 IO 的方向可以用它与三个主轴的夹角 $(\zeta, \eta, \theta)$ 来表示。由于 IO 是空间中的一条直线，其方向单位矢量为 $\hat{n} = (\cos\zeta, \cos\eta, \cos\theta)$。单位矢量的模为1，因此 $\cos^2\zeta + \cos^2\eta + \cos^2\theta = 1$。这是三个变量之间的约束关系，独立的变量只有两个。

角速度 $\alpha'$ 也是变量，其变化由以下关系给出：

$$\frac{d\alpha'}{\alpha'} = \frac{(aa-bb)(aa-cc)(bb-cc)}{aabbcc}\cos\zeta\cos\eta\cos\theta\,dt$$

**推理过程**：这个公式可以从自由旋转的欧拉方程推导出来。设角速度的分量为 $\omega_1 = \alpha'\cos\zeta$，$\omega_2 = \alpha'\cos\eta$，$\omega_3 = \alpha'\cos\theta$，自由旋转的欧拉方程为：$I_1\dot{\omega}_1 = (I_2-I_3)\omega_2\omega_3$，等。将 $\omega_i = \alpha'\cos(\cdot)$ 代入并对时间微分，利用约束条件消去 $d\zeta, d\eta, d\theta$，经过复杂的代数运算后可以得到 $\alpha'$ 的变化率公式。关键步骤是将三个欧拉方程分别乘以 $2\omega_1, 2\omega_2, 2\omega_3$ 并适当组合，最终消去所有 $d\zeta, d\eta, d\theta$ 项。

弧 $\zeta$, $\eta$, $\theta$ 的变化由三个方程确定。经过变换后，这些方程可以化为可积分的形式：

$$\frac{d\zeta}{\sin\zeta\cos\zeta} = \frac{-d\alpha'}{\alpha'(aa-bb)(aa-cc)}\left[bbcc - (bb-aa)(cc-aa)\cos^2\zeta\right]$$

（以及类似的关于 $\eta$ 和 $\theta$ 的方程）

**推理过程**：从欧拉方程 $Maa \cdot d(\alpha'\cos\zeta) = (Mbb - Mcc)\alpha'^2\cos\eta\cos\theta\,dt$ 出发，展开左边的微分：$aa(\cos\zeta\,d\alpha' - \alpha'\sin\zeta\,d\zeta) = (bb-cc)\alpha'^2\cos\eta\cos\theta\,dt$。利用约束条件 $\cos^2\zeta + \cos^2\eta + \cos^2\theta = 1$ 消去 $\cos\eta\cos\theta$ 项（通过两个欧拉方程的适当组合），可以将 $d\zeta$ 表示为仅含 $\alpha'$, $d\alpha'$, $\zeta$ 和常数的表达式。这正是上面给出的可积形式。

这些方程的积分给出三个守恒量：

$$\text{I.}\quad \frac{A}{\alpha'^2} = bbcc - (bb-aa)(cc-aa)\cos^2\zeta$$

$$\text{II.}\quad \frac{B}{\alpha'^2} = aacc - (cc-bb)(aa-bb)\cos^2\eta$$

$$\text{III.}\quad \frac{C}{\alpha'^2} = aabb - (aa-cc)(bb-cc)\cos^2\theta$$

其中常数 A, B, C 满足关系 $A(cc-bb) + B(aa-cc) + C(bb-aa) = 0$。

**推导**：这些守恒量实际上来自角动量守恒和能量守恒。动能（活力）为 $2T = Maa\omega_1^2 + Mbb\omega_2^2 + Mcc\omega_3^2 = M\alpha'^2(aa\cos^2\zeta + bb\cos^2\eta + cc\cos^2\theta)$。角动量的三个分量为 $L_i = I_i\omega_i = M\alpha'\cdot(\cdot)\cos(\cdot)$。守恒量 I 来自 $L_1^2/(Maa)$ 的某种组合，具体是将角动量平方 $L^2 = L_1^2 + L_2^2 + L_3^2$ 和 $2T$ 适当线性组合后消去 $\cos^2\eta$ 和 $\cos^2\theta$ 项。具体地：$L^2/M = aa\alpha'^2\cos^2\zeta \cdot aa + bb\alpha'^2\cos^2\eta \cdot bb + cc\alpha'^2\cos^2\theta \cdot cc$（注：这里 $L_i = Ma\alpha'\cos\zeta \cdot a$ 等），$2T/M = \alpha'^2(aa\cos^2\zeta + bb\cos^2\eta + cc\cos^2\theta)$。从这两个守恒量出发，加上约束条件 $\cos^2\zeta + \cos^2\eta + \cos^2\theta = 1$，可以消去任意两个变量，得到仅含一个变量的关系——这就是守恒量 I, II, III。关系 $A(cc-bb) + B(aa-cc) + C(bb-aa) = 0$ 则是因为三个守恒量中只有两个是独立的。

> **批注**：这三个守恒量实际上对应于角动量守恒（三个分量）和能量守恒的组合。在现代力学中，自由旋转有两个独立的守恒量——角动量大小 $L^2 = I_1^2\omega_1^2 + I_2^2\omega_2^2 + I_3^2\omega_3^2$ 和动能 $2T = I_1\omega_1^2 + I_2\omega_2^2 + I_3\omega_3^2$——它们将角速度限制在惯量椭球和角动量椭球的交线上。欧拉在这里使用的参数化（用角速度和球面坐标弧来描述转动轴）是等效的。

---

## 问题77

（续）从给定的初始转动轴和角速度出发，求任意时刻的角速度和转动轴位置。

### 解

设 IE 为初始转动轴（弧 AE = a, BE = b, CE = c），初始角速度为 $\alpha$。设 aa 为最大值，bb 为中间值，cc 为最小值。

定义常数：

$$\mathcal{A} = \frac{bbcc}{(aa-bb)(aa-cc)}, \quad \mathcal{B} = \frac{aacc}{(aa-bb)(bb-cc)}, \quad \mathcal{C} = \frac{aabb}{(aa-cc)(bb-cc)}$$

满足 $\mathcal{A} + \mathcal{B} + \mathcal{C} = 1$。

**推导**：将 $\mathcal{A} + \mathcal{B} + \mathcal{C}$ 通分，公分母为 $(aa-bb)(aa-cc)(bb-cc)$，分子为 $bbcc(bb-cc) + aacc(cc-aa) + aabb(aa-bb) = bb^2cc - bbcc^2 + aacc^2 - aa^2cc + a^2bb - aabb = 0$（仔细展开后各项成对消去），所以和为1。更直观地，这些常数来自部分分式分解：将 $\frac{1}{(x-aa)(x-bb)(x-cc)}$ 分解为 $\frac{\mathcal{A}}{x-aa} + \frac{\mathcal{B}}{x-bb} + \frac{\mathcal{C}}{x-cc}$ 的形式。

定义 $D = \frac{aabbcc}{(aa-bb)(aa-cc)(bb-cc)}$。

引入变量 $v$ 使得 $\alpha' = \alpha(1+v)$，则时间方程为：

$$2\,dt = \frac{D\,dv}{\sqrt{(\cos^2 a + \mathcal{A}v)(\cos^2 b - \mathcal{B}v)(\cos^2 c + \mathcal{C}v)}}$$

**推理过程**：从问题76的三个守恒量出发，消去 $\zeta$ 和 $\theta$，可以得到 $\cos^2\eta$ 作为 $\alpha'$（从而 $v$）的函数。将 $d\eta/dt$ 用 $v$ 表示，利用 $d\eta$ 的表达式从守恒量中解出 $dt$ 作为 $dv$ 的函数。被积函数中出现的三次多项式的平方根来自三个守恒量的乘积，这确实是椭圆积分的标准形式。具体地，从守恒量 I：$\cos^2\zeta = (bbcc - A/\alpha'^2)/((bb-aa)(cc-aa))$，类似得到 $\cos^2\eta$ 和 $\cos^2\theta$。代入 $\alpha' = \alpha(1+v)$ 和初始条件，即得 $\cos^2\zeta = (\cos^2 a + \mathcal{A}v)/(1+v)$ 等。将 $dt$ 表示为 $dv$ 的函数时，被积函数的分母是这三个表达式乘积的平方根。

积分条件为：当 t = 0 时 v = 0。知道 v 在时间 t 的值后，角速度和转动轴位置分别为：

$$\alpha' = \alpha(1+v)$$

$$\cos\zeta = \sqrt{\frac{\cos^2 a + \mathcal{A}v}{1+v}}, \quad \cos\eta = \sqrt{\frac{\cos^2 b - \mathcal{B}v}{1+v}}, \quad \cos\theta = \sqrt{\frac{\cos^2 c + \mathcal{C}v}{1+v}}$$

**推理过程**：这些表达式直接来自守恒量公式。以 $\cos\zeta$ 为例：守恒量 I 给出 $bbcc - (bb-aa)(cc-aa)\cos^2\zeta = A/\alpha'^2$。初始条件 $t=0$ 时给出 $A = \alpha^2[bbcc - (bb-aa)(cc-aa)\cos^2 a]$。设 $\alpha' = \alpha(1+v)$，代入后：$bbcc - (bb-aa)(cc-aa)\cos^2\zeta = [bbcc - (bb-aa)(cc-aa)\cos^2 a]/(1+v)^2$。解出 $\cos^2\zeta = [(bb-aa)(cc-aa)\cos^2 a \cdot v/(1+v) + bbcc \cdot ((1+v)^2-1)/(1+v)^2] / ((bb-aa)(cc-aa))$。利用 $\mathcal{A} = bbcc/((aa-bb)(aa-cc))$，经过化简即得 $\cos^2\zeta = (\cos^2 a + \mathcal{A}v)/(1+v)$。其余两个公式类似推导。

---

## 推论1

**738.** 除非刚体的两个转动惯量相等，绕可变轴的转动运动不是匀速的；确定任意时刻的角速度似乎是极为困难的。

## 推论2

**739.** 但在求出时间 t 的角速度 $\alpha'$ 后，转动轴相对于主轴的位置可以容易地从弧 $\zeta$, $\eta$, $\theta$ 的公式确定。

## 推论（741-743）

**741.** 如果初始弧 a, b, c 中有一个为零（其余为象限），则转动轴落在某个主轴上，刚体绕之匀速转动。

**推理过程**：如果 a = 0，则 $\cos a = 1$，$\cos b = \cos c = 0$。代入 $\mathcal{A}, \mathcal{B}, \mathcal{C}$ 的表达式和时间方程，得 $\cos^2 a + \mathcal{A}v = 1 + \mathcal{A}v$，$\cos^2 b - \mathcal{B}v = -\mathcal{B}v$，$\cos^2 c + \mathcal{C}v = \mathcal{C}v$。要使表达式有物理意义（根号内非负），需要 $v = 0$。因此 $\alpha' = \alpha$（角速度不变），$\cos\zeta = 1$，$\cos\eta = \cos\theta = 0$（转动轴始终沿 IA 方向）。

**742.** 角速度在转动极点 O 位于 ABC 区域（三个余弦为正）时增加。

**743.** 如果转动极点落在象限 ABb, BCc, CAa 区域，角速度减小；但在象限 Aa, Bb, Cc 区域则增加。

**推理过程**：由角速度变化公式 $\frac{d\alpha'}{\alpha'} = \frac{(aa-bb)(aa-cc)(bb-cc)}{aabbcc}\cos\zeta\cos\eta\cos\theta\,dt$，角速度的增减取决于因子 $(aa-bb)(aa-cc)(bb-cc)\cos\zeta\cos\eta\cos\theta$ 的符号。假设 $aa > bb > cc$，则 $(aa-bb)(aa-cc)(bb-cc) > 0$，角速度的增减仅取决于 $\cos\zeta\cos\eta\cos\theta$ 的符号。在 ABC 区域三个余弦都为正，乘积为正，角速度增加。在其他区域，某些余弦为负，乘积可能为负，角速度减小。

> **批注**：角速度的增减与转动轴位置的关系揭示了一个有趣的物理图景。转动轴在球面上移动时，当它靠近中间惯量轴的区域时角速度变化最快。这正是中间轴不稳定性的体现——在下一章（注释部分）欧拉将更详细地讨论稳定性问题。

---

## 注释1（744）

**744.** 值得注意的是，待积分的公式不能通过代数函数、圆弧（三角函数）或对数来求值——我们被迫承认这个积分需要通过求积法（quadrature）来完成。

> **批注**：欧拉在这里明确承认了这个问题的数学困难。时间方程中被积函数的分母是 v 的三次多项式的平方根，这正是椭圆积分的标准形式。欧拉意识到这种积分超越了当时已知的初等函数范畴。事实上，这类积分后来被勒让德系统研究（1786年），并被阿贝尔和雅可比在1820年代末发展为椭圆函数理论。从现代观点看，三轴不等体自由旋转的所有运动学量都可以用雅可比椭圆函数和椭圆积分精确表达。

---

## 注释——稳定性讨论

欧拉在分析三轴不等体的运动时，隐含地触及了**绕主轴旋转的稳定性**问题：

- 如果转动轴恰好落在最大惯量轴（aa 最大）或最小惯量轴（cc 最小）上，运动是稳定的——微小扰动只引起转动轴的微小振荡。
- 如果转动轴落在中间惯量轴（bb）上，运动是**不稳定的**——微小扰动将使转动轴远离中间轴。

**推理过程（稳定性分析）**：设转动轴初始沿主轴 $IA$，即 $\cos\zeta = 1$，$\cos\eta = \cos\theta = 0$。施加微小扰动后，$\cos\zeta \approx 1$，$\cos\eta \approx \epsilon$，$\cos\theta \approx \delta$（$\epsilon, \delta$ 很小）。将欧拉方程线性化，得到关于 $\epsilon$ 和 $\delta$ 的线性耦合方程：$Mbb\,\dot{\epsilon} = (Mcc - Maa)\omega_1\delta$，$Mcc\,\dot{\delta} = (Maa - Mbb)\omega_1\epsilon$。消元后得 $\ddot{\epsilon} = -\frac{(aa-bb)(aa-cc)}{bbcc}\omega_1^2 \epsilon$。

对于最大惯量轴（$aa > bb > cc$）：$(aa-bb)(aa-cc) > 0$，所以 $\ddot{\epsilon} \propto -\omega_1^2\epsilon < 0$，这是简谐振动方程，$\epsilon$ 做小幅振荡——**稳定**。

对于最小惯量轴（$cc < bb < aa$）：$(aa-cc)(bb-cc) > 0$，类似分析也得 $\ddot{\epsilon} \propto -\omega_1^2\epsilon < 0$——**稳定**。

对于中间惯量轴（$aa > bb > cc$，绕 $IB$ 扰动）：$(aa-bb)(bb-cc) > 0$ 但对应的有效系数为 $\frac{(aa-bb)(bb-cc)}{aacc}$，线性化后得到 $\ddot{\epsilon} \propto +\omega_1^2\epsilon > 0$，这是指数增长方程——**不稳定**。

> **批注**：这就是后来被称为"网球拍定理"或"中间轴定理"的内容的原始来源。虽然欧拉在本章中的分析主要是运动学的（描述运动"是什么"），而非动力学的（解释"为什么"），但他建立的运动方程已经包含了稳定性分析的种子。完整的稳定性分析后来由庞加莱和其他人更严格地完成。

---

## 本章小结

第13章处理了最一般的自由旋转——三轴不等体的自由运动：

1. **基本方程**：转动轴位置和角速度的变化由三个耦合的微分方程描述
2. **守恒量**：角动量和能量提供了三个积分关系
3. **时间方程**：最终归结为椭圆积分——超越初等函数
4. **角速度的非匀速性**：三轴不等体的角速度不断变化
5. **稳定性暗示**：绕中间惯量轴的旋转是不稳定的

> **批注**：本章是欧拉自由旋转理论的高潮，也是他遇到数学困难的转折点。三轴不等体的自由旋转无法用初等方法解决，这直接推动了19世纪椭圆函数理论的发展。从更广泛的历史视角看，这个问题具有深刻的意义：它表明，即使是最基本的物理系统（不受力的刚体），其运动也可能需要高级的数学工具来描述。这种"物理简单性与数学复杂性"的反差，是经典力学乃至整个理论物理的反复出现的主题。欧拉在这里建立的运动方程和分析方法，经过勒让德、泊松、雅可比等人的发展，最终形成了我们今天所知的刚体自由旋转的完整理论。

---

[上一章：正文第12章——两主轴相等刚体不受力时的自由运动](欧拉刚体运动理论_正文_第12章.md)

[下一章：正文第14章——陀螺在水平面上的运动](欧拉刚体运动理论_正文_第14章.md)
