# 正文第7章：主轴的寻找

*Concerning the Principal Axes*

---

## 本章概述

这是全书最伟大的章节之一。欧拉在这里证明了**主轴定理**（principal axes theorem）：对于任何刚体，都存在三个相互垂直的轴——**主轴**（principal axes），使得关于这些轴的惯量积为零。用现代语言说，他证明了任何实对称矩阵都可以通过正交变换对角化——这是线性代数中"谱定理"（spectral theorem）的一个特例，比矩阵论的诞生早了近一个世纪。

> **批注**：主轴定理对应Ian Bruce英译本卷一第5章中的定义8（Definitio 8）："The principal axes of any body are these three axes passing through the centre of inertia of this body, with respect to which the moments of inertia are either a maximum or a minimum." 欧拉通过物理动机（简化旋转运动方程）发现了这个代数定理。值得注意的历史对比是：拉格朗日后来在《分析力学》（1788）中也独立地处理了类似问题，但欧拉在此处的工作（1765年）更具原创性——他首次系统地将极值方法应用于惯量张量的对角化。

---

## 1. 问题的提出

在第6章中，转动惯量关于任意方向 $\hat{\mathbf{n}}$ 的表达式为：
$$I(\hat{\mathbf{n}}) = A\alpha^2 + B\beta^2 + C\gamma^2 - 2D\alpha\beta - 2E\alpha\gamma - 2F\beta\gamma$$

这个表达式中，惯量积 $D, E, F$ 的存在使得公式复杂化。欧拉问：

**是否可以选择一个特殊的坐标系，使得所有惯量积都为零？**

即：是否存在方向 $(\alpha, \beta, \gamma)$，使得新坐标系中 $D' = E' = F' = 0$？

## 2. 极值条件

欧拉的天才洞察是：**惯量积为零的轴（principal axes），正是使转动惯量（moment of inertia）取极值的轴。**

**为什么惯量积为零等价于转动惯量取极值？** 直观地理解：如果某个方向使转动惯量取极大（或极小），那么在该方向上做微小变化时，转动惯量的一阶变化为零。这意味着惯量椭球在该方向的法线平行于径向——这正是惯量积为零的几何含义。

因此，问题等价于：在约束条件 $\alpha^2 + \beta^2 + \gamma^2 = 1$（方向余弦的归一化条件）下，求 $I(\hat{\mathbf{n}})$ 的极值。

用拉格朗日乘子法（虽然欧拉使用的是直接微分法）：

$$\frac{\partial}{\partial\alpha}[I(\hat{\mathbf{n}}) - \lambda(\alpha^2 + \beta^2 + \gamma^2 - 1)] = 0$$

对 $\alpha$ 求偏导：
$$2A\alpha - 2D\beta - 2E\gamma - 2\lambda\alpha = 0$$

同理对 $\beta$ 和 $\gamma$ 求偏导，得到三个方程：
$$2A\alpha - 2D\beta - 2E\gamma = 2\lambda\alpha$$
$$-2D\alpha + 2B\beta - 2F\gamma = 2\lambda\beta$$
$$-2E\alpha - 2F\beta + 2C\gamma = 2\lambda\gamma$$

整理为：
$$\begin{cases}(A - \lambda)\alpha - D\beta - E\gamma = 0 \\ -D\alpha + (B - \lambda)\beta - F\gamma = 0 \\ -E\alpha - F\beta + (C - \lambda)\gamma = 0\end{cases}$$

> **批注**：这就是特征值问题！用现代矩阵符号：
> $$\mathbf{I}\hat{\mathbf{n}} = \lambda\hat{\mathbf{n}}$$
>
> 拉格朗日乘子 $\lambda$ 就是惯量张量矩阵 $\mathbf{I}$ 的特征值，而对应的方向 $(\alpha, \beta, \gamma)$ 就是特征向量。欧拉虽然没有使用矩阵语言，但他清楚地列出了特征方程。

## 3. 特征方程

上述方程组有非零解的条件是系数行列式为零：

$$\begin{vmatrix} A - \lambda & -D & -E \\ -D & B - \lambda & -F \\ -E & -F & C - \lambda \end{vmatrix} = 0$$

展开这个行列式。按第一行展开：
$$(A-\lambda)[(B-\lambda)(C-\lambda) - F^2] + D[-D(C-\lambda) - (-E)(-F)] + (-E)[(-D)(-F) - (-E)(B-\lambda)] = 0$$

$$= (A-\lambda)[(B-\lambda)(C-\lambda) - F^2] - D[D(C-\lambda) + EF] - E[DF - E(B-\lambda)] = 0$$

进一步展开并合并 $\lambda$ 的同次项，最终得到一个关于 $\lambda$ 的三次方程：

$$\lambda^3 - (A+B+C)\lambda^2 + (AB+BC+CA - D^2 - E^2 - F^2)\lambda - (ABC + 2DEF - AE^2 - BF^2 - CD^2) = 0$$

> **批注**：欧拉证明了这三个根都是实数——这是关键一步。用现代语言，这就是说实对称矩阵的特征值都是实数。欧拉的证明基于以下观察：惯量张量矩阵 $\mathbf{I}$ 是对称的（$D, E, F$ 在非对角位置对称出现），而二次型 $I(\hat{\mathbf{n}})$ 在单位球面上是连续的，必然在某个方向取最大值、某个方向取最小值。

## 4. 三个主轴的存在

欧拉证明了：

**定理（主轴定理 / Principal Axes Theorem）**：对于任何刚体，都存在三个相互垂直的方向 $\hat{\mathbf{n}}_1, \hat{\mathbf{n}}_2, \hat{\mathbf{n}}_3$，使得惯量积为零。关于这三个方向的转动惯量 $I_1, I_2, I_3$ 称为主转动惯量（principal moments of inertia）。

### 证明要点

1. 三次方程有三个根（至少一个实的，用连续性论证可证全部是实的）。

**为什么三个根都是实数？** 设 $\lambda$ 是一个复根，$\hat{\mathbf{n}}$ 是对应的复特征向量。由于 $\mathbf{I}$ 是实对称矩阵：
$$\mathbf{I}\hat{\mathbf{n}} = \lambda\hat{\mathbf{n}} \implies \hat{\mathbf{n}}^*\mathbf{I}\hat{\mathbf{n}} = \lambda\hat{\mathbf{n}}^*\hat{\mathbf{n}}$$
$$\implies \lambda = \frac{\hat{\mathbf{n}}^*\mathbf{I}\hat{\mathbf{n}}}{\hat{\mathbf{n}}^*\hat{\mathbf{n}}}$$

由于 $\mathbf{I}$ 是实对称的，$\hat{\mathbf{n}}^*\mathbf{I}\hat{\mathbf{n}}$ 是实数（因为 $(\hat{\mathbf{n}}^*\mathbf{I}\hat{\mathbf{n}})^* = \hat{\mathbf{n}}^T\mathbf{I}\hat{\mathbf{n}}^* = \hat{\mathbf{n}}^*\mathbf{I}\hat{\mathbf{n}}$），而 $\hat{\mathbf{n}}^*\hat{\mathbf{n}}$ 也是实数且为正。因此 $\lambda$ 是实数。

2. 设这三个根为 $\lambda_1 \leq \lambda_2 \leq \lambda_3$。
3. 每个根对应一个特征方向（主轴方向）。
4. 不同特征值对应的特征向量互相垂直。

**证明正交性**：设 $\mathbf{I}\hat{\mathbf{n}}_1 = \lambda_1\hat{\mathbf{n}}_1$ 和 $\mathbf{I}\hat{\mathbf{n}}_2 = \lambda_2\hat{\mathbf{n}}_2$，且 $\lambda_1 \neq \lambda_2$。则：
$$\hat{\mathbf{n}}_2^T(\mathbf{I}\hat{\mathbf{n}}_1) = \lambda_1\hat{\mathbf{n}}_2^T\hat{\mathbf{n}}_1$$
$$(\hat{\mathbf{n}}_2^T\mathbf{I})\hat{\mathbf{n}}_1 = (\mathbf{I}\hat{\mathbf{n}}_2)^T\hat{\mathbf{n}}_1 = \lambda_2\hat{\mathbf{n}}_2^T\hat{\mathbf{n}}_1$$

两式相减：$(\lambda_1 - \lambda_2)\hat{\mathbf{n}}_2^T\hat{\mathbf{n}}_1 = 0$。由于 $\lambda_1 \neq \lambda_2$，必有 $\hat{\mathbf{n}}_1 \cdot \hat{\mathbf{n}}_2 = 0$。

5. 当三个特征值互不相同时，主轴被唯一确定。
6. 当两个特征值相同时，对应的两个主轴可以在一个平面内任意选择。
7. 当三个特征值都相同时（如均匀球体），任何三个互相垂直的方向都可以作为主轴。

$\blacksquare$

> **批注**：欧拉对主轴定理的证明是18世纪数学最优美的结果之一。他的证明策略值得注意：先在问题27中通过一个三次方程（即特征方程）找到第一个主轴，然后在问题28中利用已知一个主轴的条件下简单地得到另外两个，它们都与第一个主轴垂直。在注解（Scholion）445中，欧拉特别指出这个结果不仅适用于质心，对任意点也成立——这是一个更一般性的结果。从数学史的角度，这个结果实际上等价于实对称矩阵的正交对角化，而后者直到19世纪才由柯西（Cauchy）和魏尔斯特拉斯（Weierstrass）严格证明。

## 5. 主转动惯量的性质

### 排序
三个主转动惯量可以排列为 $I_1 \leq I_2 \leq I_3$。

### 三角不等式
$$I_1 + I_2 \geq I_3$$

**推导**：由转动惯量的定义 $I_3 = \sum m_i(x_i^2 + y_i^2)$（假设 $z$ 轴是第三主轴），以及 $I_1 = \sum m_i(y_i^2 + z_i^2)$，$I_2 = \sum m_i(x_i^2 + z_i^2)$，有：
$$I_1 + I_2 - I_3 = \sum m_i[(y_i^2 + z_i^2) + (x_i^2 + z_i^2) - (x_i^2 + y_i^2)] = 2\sum m_i z_i^2 \geq 0$$

因此 $I_1 + I_2 \geq I_3$。等号仅在所有质点都在 $z = 0$ 平面内时成立（薄片物体）。

（绕任何轴的转动惯量不能超过绕其他两个正交轴的转动惯量之和。）

### 关于质心的主轴
如果固定点选为质心，则主轴称为**中心主轴**。中心主轴有以下特殊性质：
- 绕中心主轴的转动惯量是所有平行轴中最小的。
- 中心主轴与刚体固定连接，随刚体运动。

> **批注**：三角不等式 $I_1 + I_2 \geq I_3$ 的物理意义是：不存在一个方向，使得绕它的转动惯量"过大"。这与质量分布的几何约束有关——所有质量都分布在有限的空间区域内。

## 6. 主轴坐标系中的简化

在主轴坐标系中（$x$ 轴沿 $\hat{\mathbf{n}}_1$，$y$ 轴沿 $\hat{\mathbf{n}}_2$，$z$ 轴沿 $\hat{\mathbf{n}}_3$），惯量张量变为对角形式：

$$\mathbf{I} = \begin{pmatrix} I_1 & 0 & 0 \\ 0 & I_2 & 0 \\ 0 & 0 & I_3 \end{pmatrix}$$

绕任意方向 $\hat{\mathbf{n}} = (\alpha, \beta, \gamma)$ 的转动惯量简化为：
$$I(\hat{\mathbf{n}}) = I_1\alpha^2 + I_2\beta^2 + I_3\gamma^2$$

这是因为所有惯量积 $D = E = F = 0$，交叉项全部消失。

惯量椭球的方程变为标准形式：
$$I_1 x^2 + I_2 y^2 + I_3 z^2 = 1$$

这是一个以主轴为对称轴的椭球。

> **批注**：惯量椭球（moment of inertia ellipsoid）的半轴长度分别为 $1/\sqrt{I_1}$，$1/\sqrt{I_2}$，$1/\sqrt{I_3}$。最大的转动惯量对应最短的半轴，最小的转动惯量对应最长的半轴。欧拉在问题29中给出了一个优美的一般公式：若轴 IG 与三个主轴的夹角分别为 $\alpha, \beta, \gamma$，则 $I_{IG} = I_1\cos^2\alpha + I_2\cos^2\beta + I_3\cos^2\gamma$。这个公式是惯量椭球的直接推论，也是后世所谓的"柯西惯量椭球"（Cauchy's inertia ellipsoid）的数学基础。

## 7. 具有对称性的物体的主轴

欧拉讨论了几种常见的对称情况：

### 有一个对称面的物体
对称面法线方向是一个主轴。另外两个主轴在对称面内。

**证明**：设 $x$-$y$ 平面是对称面。对于对称面一侧的每个质点 $(x, y, z)$，在对称面另一侧有等质量的质点 $(x, y, -z)$。因此 $\sum m_i x_iz_i = 0$（$xz$ 项成对抵消）和 $\sum m_i y_iz_i = 0$（$yz$ 项成对抵消），即 $E = F = 0$。但 $D = \sum m_ix_iy_i$ 一般不为零，因此 $z$ 轴（法线方向）是一个主轴，而另外两个主轴在对称面内（需要进一步对角化 $2\times 2$ 子矩阵）。

### 有两个对称面的物体
两个对称面的交线是一个主轴。三个主轴都被确定。

### 旋转对称体（如圆柱体、圆锥体）
对称轴是一个主轴。另外两个主轴在与对称轴垂直的平面内，对应的转动惯量相等（$I_2 = I_3$）。

### 球体
$I_1 = I_2 = I_3$。任何三个互相垂直的轴都是主轴。

> **批注**：对称性与主轴的关系是一个深刻的主题。在现代物理中，对称性（群论）与物理量的变换性质（表示论）之间的关系是基本范式之一。欧拉在解释（Explicatio）451中特别指出了一个重要的微妙之处：三个主轴中，一个绝对最大、一个绝对最小，而第三个是"鞍点"——它既非最大也非最小，但在极值计算中仍满足条件。这种"中间主轴"的概念在现代微分几何中对应于莫尔斯理论（Morse theory）中的鞍点。

## 8. 惯量椭球的几何解释

欧拉给出了主轴定理的一个优美的几何解释：

在惯量椭球面上取一个点 $P$，从中心 $O$ 到 $P$ 的矢量方向就是某个方向 $\hat{\mathbf{n}}$。$P$ 处椭球面的法线方向给出了**角动量方向**（当角速度沿 $\hat{\mathbf{n}}$ 时）。

**为什么法线方向是角动量方向？** 惯量椭球的方程为 $f(x,y,z) = I_1x^2 + I_2y^2 + I_3z^2 = 1$。法线方向为 $\nabla f = (2I_1x, 2I_2y, 2I_3z)$。若角速度沿 $\hat{\mathbf{n}} = (x, y, z)$（取适当比例），则角动量为 $\mathbf{L} = \mathbf{I}\hat{\mathbf{n}} = (I_1x, I_2y, I_3z)$，正是法线方向。

- 在一般情况下，角动量方向与角速度方向不同（法线不平行于径向）。
- 在主轴方向上，椭球面的法线方向与径向方向重合——这意味着**角动量与角速度同方向**。

> **批注**：这是主轴最重要的物理性质：**当刚体绕主轴旋转时，角动量矢量与角速度矢量平行。** 这意味着绕主轴的旋转是"简单的"——不需要外力矩来维持角动量方向的改变。在下一章中，欧拉将利用这一性质引入"自由轴"（free axes）的概念——即刚体在无外力作用下可绕其自由旋转的轴，这些轴恰好就是主轴。这个发现将主轴的数学性质与自由旋转的物理行为直接联系起来。

---

## 本章小结

正文第7章建立了主轴定理——刚体动力学的核心数学结果：

1. **极值条件**：惯量积为零的轴就是转动惯量取极值的轴
2. **特征值问题**：$\mathbf{I}\hat{\mathbf{n}} = \lambda\hat{\mathbf{n}}$
3. **主轴定理**：三个互相垂直的主轴存在，惯量积为零
4. **主转动惯量**：$I_1, I_2, I_3$，满足三角不等式
5. **主轴坐标系**：惯量张量对角化
6. **对称性与主轴**：对称性帮助确定主轴方向
7. **关键物理性质**：绕主轴旋转时角动量平行于角速度

> **批注**：主轴定理的建立是欧拉刚体动力学理论的关键转折点。在此之后，所有的旋转运动问题都可以在主轴坐标系中讨论，极大地简化了数学表述。从历史发展的脉络看，欧拉的主轴定理为后来拉格朗日（Lagrange）在《分析力学》中的一般化处理、以及柯西（Cauchy）在弹性力学中引入应力张量铺平了道路。主轴概念从力学延伸到了弹性理论、电磁学（主介电轴），甚至量子力学（主量子数）——尽管后者的含义已大不相同。

---

[上一章：正文第6章——物体的转动惯量](欧拉刚体运动理论_正文_第6章.md)

[下一章：正文第8章——结构恒定的刚体运动](欧拉刚体运动理论_正文_第8章.md)
