# 正文第9章：刚体运动初始生成的规律

*Concerning the Initial Generation of Motion in Rigid Bodies*

---

## 本章概述

这一章中，欧拉研究了静止的自由刚体在受力后如何开始运动——即"运动的初始生成"（initial generation of motion）。这是从静力学到动力学过渡的关键步骤。欧拉证明了一个重要定理：无论多少个力作用在刚体上，都可以等效简化为两个力，其中一个通过质心（centre of inertia）。然后他确定了刚体开始旋转的瞬时轴的方向。

> **批注**：这一章对应Ian Bruce英译本卷一第9章"Concerning the Initial Generation of Motion in Rigid Bodies"。欧拉在这里处理的问题具有深刻的物理意义——当一个静止的刚体被力击打时，它将如何开始运动？答案涉及两方面：质心的平动和绕某轴的转动。确定这个"初始旋转轴"的位置是本章的核心问题。从历史脉络看，这个问题在欧拉之前从未被系统地解决过。

---

## 1. 力的等效简化（定理6-7）

欧拉首先建立了两个基本定理。

**定理6**：如果已知两个同时作用的力产生的效果，且其中一个通过质心，则另一个单独作用的效果也可以确定。

**证明思路**：设力 $\mathbf{F}_1$ 通过质心，力 $\mathbf{F}_2$ 不通过质心。两者共同作用的平动效果由 $\mathbf{F}_1 + \mathbf{F}_2$ 决定（质心运动定理），旋转效果由 $\mathbf{F}_2$ 单独决定（因为 $\mathbf{F}_1$ 通过质心不产生力矩）。如果已知共同效果，减去 $\mathbf{F}_1$ 的贡献即可得到 $\mathbf{F}_2$ 的效果。

**定理7**：无论多少个力作用在刚体上，无论它们如何施加，**这些力总可以等效简化为两个力，其中一个通过质心**。

**证明思路**：设有 $n$ 个力 $\mathbf{F}_1, \ldots, \mathbf{F}_n$ 分别作用于点 $\mathbf{r}_1, \ldots, \mathbf{r}_n$。

**步骤1**：计算合力 $\mathbf{F}_{\text{total}} = \sum_{i=1}^n \mathbf{F}_i$。在质心施加一个力 $\mathbf{F}_{\text{total}}$，这将产生与原力系相同的平动效果。

**步骤2**：但仅 $\mathbf{F}_{\text{total}}$ 作用于质心时不产生力矩，而原力系的总力矩为 $\boldsymbol{\tau} = \sum_{i=1}^n \mathbf{r}_i \times \mathbf{F}_i$。为了补偿力矩的差异，需要再施加一个力偶（或一个不通过质心的力），使其产生的力矩等于 $\boldsymbol{\tau}$。

**步骤3**：将 $\mathbf{F}_{\text{total}}$ 分解为两个力：$\mathbf{S} = \mathbf{F}_{\text{total}}$ 通过质心（产生平动），$\mathbf{V}$ 不通过质心（与 $\mathbf{S}$ 的适当组合产生正确的力矩）。实际上，欧拉的方法是将每个力分解为通过质心的分量和一个"剩余"分量，然后将所有"剩余"分量合成第二个力。

> **批注**：定理7是静力学中"力系的简化"在动力学中的推广。在现代力学中，这等价于说：任何力系可以简化为一个通过质心的力（决定平动）和一个力偶（决定转动）。欧拉的证明方法非常精巧——他通过反复使用力的平行分解，将每个力都分解为通过质心的分量和另一分量，最后将所有分量合成。这种方法虽然不如后来的矢量方法简洁，但清晰地展示了几何直觉。

## 2. 施加力使刚体绕指定轴旋转（问题57）

欧拉在问题57中提出并解决了：如何施加两个力（一个通过质心），使刚体开始绕指定的通过质心的轴旋转？

设旋转轴为 IA，欧拉证明：
- 一个力施加在质心 I
- 另一个等大反向的力施加在轴外一点 R
- 距离 $AR = \frac{a \cdot \sum rr\,dM}{\sum xy\,dM}$（其中 $a = IA$ 为可任选的长度）

**推导思路**：力偶 $V$ 产生力矩 $M = V \cdot AR$。要使刚体绕轴 IA 以角加速度 $\dot{\omega}$ 旋转，需要 $I_A\dot{\omega} = M$，即 $I_A\dot{\omega} = V \cdot AR$。由此 $AR = I_A\dot{\omega}/V$。但 $I_A = \sum r^2\,dM$，而角度关系给出了 $AR$ 与惯量积 $\sum xy\,dM$ 的联系——力偶不仅产生绕 IA 的旋转，还会产生绕其他轴的旋转分量，除非力的作用线满足特定条件。

所有满足条件的点 R 都在一条直线上，这条直线与旋转轴 IA 的夹角的正切等于 $\frac{\sum rr\,dM}{\sum xy\,dM}$。

> **批注**：问题57的结论表明，对于给定的旋转轴，存在一个特定的"打击线"（line of percussion），沿这条线施加力（同时在质心施加反向力）将产生绕该轴的纯旋转。这个概念后来在冲击理论中发展为"打击中心"（centre of percussion）的概念——这是振动理论和钟表设计中的重要概念。

## 3. 确定初始旋转轴（问题58-59）

这是本章的核心问题。给定一个力，刚体将绕哪条轴开始旋转？

### 问题58：限制情况

如果假设旋转轴位于与力方向垂直的平面内，则问题只有在特殊条件下才有解——需要满足：
$$\frac{E}{F} + \frac{B+C}{D} = 0$$

其中 $A, B, C, D, E, F$ 是惯量张量的六个分量。

**为什么一般无解？** 给定一个力（其方向确定了一个平面），要求旋转轴在这个平面内，意味着旋转轴有2个自由度（平面内的方向角）。而旋转运动方程提供了3个约束（三个主轴方向的力矩分量必须满足特定关系）。2个自由度一般无法满足3个约束，因此只有特殊情况下才有解。

### 问题59：一般情况

欧拉取消了上述限制，允许旋转轴在任何方向。设力的方向垂直于参考平面，刚体质心为 I，力作用点到质心的距离为 $h$。

设旋转轴 ID 与参考平面中两条基线的夹角分别为 $\alpha$ 和 $\beta$，则这两个角度由以下公式确定：

$$\tan\alpha = \frac{EE - (A+B)(B+C)}{(A+B)D + EF}$$

$$\tan\beta = \frac{E\cos\alpha - F}{(A+B)\sin\alpha}$$

**推导思路**：初始旋转轴由以下条件确定——力产生的力矩方向必须与角加速度方向一致（即力矩沿旋转轴方向）。这要求：

1. 力矩矢量 $\boldsymbol{\tau} = \mathbf{r} \times \mathbf{F}$ 的方向必须平行于角加速度方向。

2. 角加速度与力矩的关系为 $\boldsymbol{\alpha} = \mathbf{I}^{-1}\boldsymbol{\tau}$（在主轴坐标系中简化为 $\alpha_i = \tau_i/I_i$）。

3. 由于 $\boldsymbol{\alpha}$ 必须平行于旋转轴 $\hat{\mathbf{n}}$，这给出两个独立的方程（交叉分量为零），足以确定 $\alpha$ 和 $\beta$。

> **批注**：问题59的解给出了确定初始旋转轴的一般方法。欧拉的推导涉及复杂的坐标变换和极值条件。值得注意的是，当旋转轴不在与力垂直的平面内时，力的分量中出现了平行于轴的分量——这是问题58的限制情况被排除的原因。从现代观点看，初始旋转轴由惯量张量的六个分量和力的作用线完全确定。

## 4. 最小活力原理（定理8）

欧拉在定理8中给出了一个优美的结果：

**如果刚体从静止开始受力（同时在质心施加等大反向的力），则初始旋转轴的选择使得整个刚体获得最小量的"活力"（living force）——即所有元素的质量乘以其速度的平方之和最小。**

**推导思路**：设旋转轴方向为 $\hat{\mathbf{n}}$，力矩为 $M$，角加速度为 $\dot{\omega} = M/I(\hat{\mathbf{n}})$。刚体获得的动能为 $T = \frac{1}{2}I(\hat{\mathbf{n}})\dot{\omega}^2$。对于给定的力矩 $M$：

$$T = \frac{1}{2}I(\hat{\mathbf{n}}) \cdot \frac{M^2}{I(\hat{\mathbf{n}})^2} = \frac{M^2}{2I(\hat{\mathbf{n}})}$$

因此动能 $T \propto 1/I(\hat{\mathbf{n}})$。最小化 $T$ 等价于最大化 $I(\hat{\mathbf{n}})$。但这里欧拉讨论的不是最大化转动惯量，而是更一般的约束优化问题——力矩也依赖于轴的方向。通过完整的变分分析，欧拉证明初始旋转轴使系统获得的总活力最小。

这是变分原理在力学中的早期应用！

> **批注**：living force（活力）是莱布尼茨引入的概念，相当于现代物理中的 $2 \times$ 动能。欧拉在这里将最小活力原理（minimum living force principle）作为确定初始旋转轴的判据。这个原理在历史发展中具有重要意义——它是最小作用量原理（principle of least action）的先驱之一，后者经过莫佩尔蒂（Maupertuis）、拉格朗日和哈密顿的发展，成为分析力学的基石。欧拉本人就是最小作用量原理的早期倡导者之一。

## 5. 利用主轴的简化（问题60）

欧拉在问题60中利用主轴来简化问题59的解。

设三个主轴为 IA, IB, IC，对应的主转动惯量为 $Maa$, $Mbb$, $Mcc$。力通过平面 AIB 的点 V（$IV = h$，角 $AIV = \varphi$），力可分解为 $P$, $Q$, $R$ 三个分量。

初始旋转轴 IF 的位置由以下公式确定：

$$\tan\varphi' = \frac{aa\cos\varphi}{bb\sin\varphi}$$

**推导**：在主轴坐标系中，惯量张量已经对角化。旋转轴在 AIB 平面内的投影方向由力矩与转动惯量的关系确定。力矩的分量正比于力臂和力的分量，而角加速度反比于对应的主转动惯量。要求角加速度方向沿旋转轴方向，给出：
$$\frac{\tau_\alpha}{Maa} = \frac{\tau_\beta}{Mbb}$$

其中 $\tau_\alpha$ 和 $\tau_\beta$ 是力矩沿两个主轴的分量。这直接导致 $\tan\varphi'$ 的表达式。

$$\tan\psi = \frac{(Q\cos\varphi - P\sin\varphi) \cdot aa\cos\varphi'}{R \cdot cc\sin\varphi'}$$

其中 $\varphi'$ 是平面 AIB 中的角度，$\psi$ 是旋转轴与该平面的夹角。

角速度的变化率：
$$d\omega = \frac{Rgh\,dt^2 \sin(\varphi + \varphi')}{M\cos\varphi' \cdot (aa\cos^2\varphi + bb\sin^2\varphi)}$$

> **批注**：问题60的解展示了主轴坐标系的巨大威力——公式显著简化。特别值得注意的是推论640：旋转轴在平面 AIB 中的投影方向只取决于力的作用点的位置（角 $\varphi$），而不取决于力的大小和方向！这个结果意味着，无论力 $V$ 的方向如何，只要它通过同一个点 $V$，旋转轴的投影方向就相同。这是一个非平凡的几何性质。

## 6. 完整的初始运动问题（问题61）

最后，欧拉在问题61中给出了完整的解决方案：

1. 将所有作用力简化为两个力 $S$（通过质心）和 $V$
2. 单独考虑力 $V$（在质心施加等大反向力），确定初始旋转轴和角加速度
3. 力 $S$ 产生平动，其加速度为 $\frac{Sg\,dt^2}{M}$
4. 将平动与转动叠加，得到完整的初始运动

三种情况：
- **$V = 0$**：只有平动（所有力的合力通过质心）
- **$S = -V$**：只有转动（力的矢量和为零）
- **一般情况**：混合运动（平动 + 转动）

**推导**：对于一般情况，力 $S$ 产生质心加速度 $\mathbf{a}_{\text{cm}} = \mathbf{S}/M$（由质心运动定理）。力 $V$（在质心施加反向力后形成力偶）产生绕初始旋转轴的角加速度 $\dot{\omega} = M_V/I$（其中 $M_V$ 是力偶矩，$I$ 是绕初始旋转轴的转动惯量）。两者独立叠加。

> **批注**：问题61总结了本章的完整逻辑链条：力系的简化 $\to$ 初始旋转轴的确定 $\to$ 平动与转动的叠加。从方法论的角度看，欧拉的"从特殊到一般"的推进策略非常清晰：先处理力通过质心的特殊情况（纯平动），再处理力不通过质心的一般情况（混合运动）。这种方法论后来被拉格朗日在《分析力学》中系统化为一般原理。

---

## 本章小结

正文第9章建立了刚体运动初始生成的完整理论：

1. **力的简化**：任意力系可简化为通过质心的力 + 力偶
2. **初始旋转轴的确定**：由惯量张量和力的作用线决定
3. **最小活力原理**：初始旋转轴使刚体获得最小动能
4. **主轴坐标系中的简化**：公式在主轴坐标系中最简
5. **运动的叠加**：平动和转动可独立确定后叠加

> **批注**：这一章标志着从运动学（kinematics）到动力学（dynamics）的过渡。前几章建立的是数学工具（转动惯量、主轴、坐标变换），而本章首次将这些工具应用于实际的物理问题——力如何产生运动。这个过渡的完成，为最终推导欧拉旋转方程（下一章）铺平了道路。从思想史的角度看，最小活力原理的引入特别值得关注——它预示了分析力学的方向。

---

[上一章：正文第8章——自由轴与刚体绕自由轴的运动](欧拉刚体运动理论_正文_第8章.md)

[下一章：正文第10章——受任意力作用的刚体自由运动](欧拉刚体运动理论_正文_第10章.md)
