# 正文第4章：受任意力作用下的旋转

*Concerning the Disturbance of Rotational Motion Arising from Forces of any Kind*

---

## 本章概述

这一章中，欧拉将第2-3章的结果推广到更一般的受力情况。他处理了各种类型的力（重力、弹性力、碰撞力等）对绕固定轴旋转的刚体的影响，并研究了振荡运动（物理摆）等具体问题。

> **批注**：这一章看似只是第3章的简单推广，但实际上它引入了一个关键的新概念——旋转运动的"平衡"和"稳定"。这些概念在后面的陀螺理论和天体力学中极为重要。在第398-407节中，欧拉通过"基本力"（elementary forces）的概念将第3章的惯性旋转推广到受力情况，推导出了角加速度与力矩的关系。

---

## 1. 一般力作用下的旋转方程

对于绕固定轴旋转的刚体，受任意力系作用，运动方程仍为：
$$I\dot{\omega} = M_{\text{外}}$$

但现在 $M_{\text{外}}$ 可以是角度 $\theta$ 的函数（如重力力矩）、角速度 $\omega$ 的函数（如阻力力矩）或时间的函数。

### 重力力矩
如果刚体受重力作用，旋转轴水平放置，则重力力矩推导如下。设质心到旋转轴的距离为 $d$，质心相对于轴正下方位置的角度为 $\theta$。重力 $Mg$ 竖直向下，其对轴的力臂为 $d\sin\theta$（质心偏离平衡位置的横向距离），因此力矩为：
$$M = -Mgd\sin\theta$$

负号表示力矩倾向于使 $\theta$ 减小（恢复力矩）。当 $\theta > 0$ 时，力矩为负（倾向于减小 $\theta$）；当 $\theta < 0$ 时，力矩为正（倾向于增大 $\theta$）。

运动方程：
$$I\ddot{\theta} = -Mgd\sin\theta$$

> **批注**：这个方程是物理摆（复摆）的运动方程。当角度 $\theta$ 很小时，$\sin\theta \approx \theta$，方程变为简谐运动方程：
> $$I\ddot{\theta} = -Mgd\theta$$
> 这是一个常系数二阶线性齐次常微分方程。设解的形式为 $\theta = A\cos(\Omega t) + B\sin(\Omega t)$，代入方程得 $-I\Omega^2\theta = -Mgd\theta$，因此 $\Omega^2 = \frac{Mgd}{I}$，即 $\Omega = \sqrt{Mgd/I}$。通解为 $\theta(t) = \theta_0\cos(\Omega t + \phi)$，其中 $\theta_0$ 和 $\phi$ 由初始条件决定。

## 2. 物理摆（复摆）

欧拉详细分析了物理摆的行为。

### 小角度近似
当 $\theta$ 很小时，$\sin\theta \approx \theta$，运动方程线性化：
$$\ddot{\theta} + \frac{Mgd}{I}\theta = 0$$

这是简谐运动方程，其特征方程为 $r^2 + \frac{Mgd}{I} = 0$，特征根为 $r = \pm i\sqrt{\frac{Mgd}{I}}$，因此通解为简谐函数，周期为：
$$T = \frac{2\pi}{\sqrt{Mgd/I}} = 2\pi\sqrt{\frac{I}{Mgd}}$$

### 等效单摆长度
物理摆的振动周期与一个长度为 $\ell$ 的单摆相同。单摆的周期公式为 $T = 2\pi\sqrt{\ell/g}$，令两者相等：
$$2\pi\sqrt{\frac{I}{Mgd}} = 2\pi\sqrt{\frac{\ell}{g}}$$

两边平方并消去公因子：
$$\frac{I}{Mgd} = \frac{\ell}{g}$$

因此：
$$\ell = \frac{I}{Md}$$

这个 $\ell$ 称为**等效单摆长度**（或简化长度）。

利用平行轴定理 $I = I_{\text{cm}} + Md^2$：
$$\ell = \frac{I_{\text{cm}} + Md^2}{Md} = \frac{I_{\text{cm}}}{Md} + d$$

> **批注**：等效单摆长度的概念使得物理摆的运动可以与单摆直接比较。注意 $\ell > d$——等效单摆的悬点比物理摆的旋转轴更低。在 $\ell$ 处有一个特殊的点，称为**振动中心**（center of oscillation）。欧拉在第417节的注释中详细讨论了旋转轴承受力的计算——三类力（外加力、等效力、离心力）在轴的两端各产生一个合力，这些公式的计算涉及多个积分 $\int ydM$、$\int zdM$、$\int xydM$、$\int xzdM$ 和 $\int rrdM$。

### 可逆性（悬点与振动中心的互换性）

欧拉证明了一个优美的性质：如果将旋转轴从原来的位置移到振动中心，新的振动周期与原来相同。

**证明**：设原旋转轴到质心距离为 $d$，等效单摆长度为 $\ell = I/(Md)$。振动中心距原旋转轴为 $\ell$，因此振动中心到质心的距离为 $\ell - d$。

现在将旋转轴移到振动中心处。新的质心到轴距离为 $d' = \ell - d$。新的转动惯量为（利用平行轴定理）：
$$I' = I_{\text{cm}} + Md'^2$$

新的等效单摆长度为：
$$\ell' = \frac{I'}{Md'} = \frac{I_{\text{cm}} + M(\ell - d)^2}{M(\ell - d)}$$

将 $I_{\text{cm}} = I - Md^2 = M(d\ell - d^2) = Md(\ell - d)$ 代入：
$$\ell' = \frac{Md(\ell - d) + M(\ell - d)^2}{M(\ell - d)} = d + (\ell - d) = \ell$$

因此新周期 $T' = 2\pi\sqrt{\ell'/g} = 2\pi\sqrt{\ell/g} = T$，周期相同。$\blacksquare$

> **批注**：这个"可逆性"是物理摆的一个迷人性质。它意味着旋转轴和振动中心可以互换而不改变周期。这个性质被用来精确测量重力加速度——通过找到同一个摆的两个等周期悬点位置，可以确定等效长度 $\ell$，再通过周期公式 $T = 2\pi\sqrt{\ell/g}$ 求出 $g$。

## 3. 大角度摆动

对于大角度摆动，$\sin\theta$ 不能近似为 $\theta$，运动方程：
$$\ddot{\theta} + \frac{Mgd}{I}\sin\theta = 0$$

是非线性的。

欧拉用首次积分（能量守恒）来分析。将运动方程两边乘以 $\dot{\theta}$：
$$\dot{\theta}\ddot{\theta} + \frac{Mgd}{I}\sin\theta \cdot \dot{\theta} = 0$$

注意到 $\dot{\theta}\ddot{\theta} = \frac{d}{dt}\left(\frac{1}{2}\dot{\theta}^2\right)$ 和 $\sin\theta\,\dot{\theta} = -\frac{d}{dt}(\cos\theta)$：
$$\frac{d}{dt}\left(\frac{1}{2}I\dot{\theta}^2\right) + \frac{d}{dt}(Mgd\cos\theta) \cdot \frac{1}{I} \cdot I = 0$$

即：
$$\frac{d}{dt}\left(\frac{1}{2}I\dot{\theta}^2 - Mgd\cos\theta\right) = 0$$

因此：
$$\frac{1}{2}I\dot{\theta}^2 + Mgd(1 - \cos\theta) = E = \text{常数}$$

其中 $E$ 是总机械能（此处将势能零点取在 $\theta = 0$ 处，故势能为 $Mgd(1-\cos\theta)$）。

由此得到：
$$\dot{\theta} = \pm\sqrt{\frac{2}{I}(E - Mgd(1 - \cos\theta))}$$

周期可以通过积分计算。从 $\theta = 0$ 到 $\theta = \theta_0$（最大摆角）所需时间为四分之一周期：
$$\frac{T}{4} = \int_0^{\theta_0}\frac{d\theta}{|\dot{\theta}|} = \sqrt{\frac{I}{2Mgd}}\int_0^{\theta_0}\frac{d\theta}{\sqrt{\cos\theta - \cos\theta_0}}$$

因此：
$$T = 4\sqrt{\frac{I}{2Mgd}}\int_0^{\theta_0}\frac{d\theta}{\sqrt{\cos\theta - \cos\theta_0}}$$

这个积分是**第一类完全椭圆积分**。具体地，令 $\sin(\theta/2) = \sin(\theta_0/2)\sin\phi$，则 $d\theta = \frac{2\sin(\theta_0/2)\cos\phi\,d\phi}{\cos(\theta/2)}$，积分化为标准形式 $T = 4\sqrt{I/(Mgd)} \cdot K(k)$，其中 $K(k) = \int_0^{\pi/2}\frac{d\phi}{\sqrt{1-k^2\sin^2\phi}}$ 是第一类完全椭圆积分，$k = \sin(\theta_0/2)$。

> **批注**：椭圆积分是欧拉在数学上做出过重大贡献的领域。他发现了椭圆积分的加法定理，这是18世纪分析数学最深刻的结果之一。在这里，物理摆的大角度运动自然地引出了椭圆积分——理论与数学的完美结合。这一事实也说明了一个重要的方法论观点：力学问题可以推动数学工具的发展。

## 4. 有阻力时的旋转运动

当旋转运动受到与角速度成正比的阻力力矩时：
$$I\dot{\omega} = M_{\text{外}} - b\omega$$

对于物理摆加阻力：
$$I\ddot{\theta} + b\dot{\theta} + Mgd\sin\theta = 0$$

小角度近似下：
$$I\ddot{\theta} + b\dot{\theta} + Mgd\theta = 0$$

这是阻尼谐振子的旋转版本。设解的形式为 $\theta = e^{rt}$，代入特征方程：
$$Ir^2 + br + Mgd = 0$$
$$r = \frac{-b \pm \sqrt{b^2 - 4IMgd}}{2I}$$

当 $b^2 < 4IMgd$ 时（欠阻尼），$r$ 为共轭复数，解为衰减振荡：
$$\theta(t) = \theta_0 e^{-bt/(2I)}\cos(\omega_d t + \phi)$$

其中阻尼振荡频率 $\omega_d = \sqrt{\frac{Mgd}{I} - \frac{b^2}{4I^2}}$。具有与引言中讨论的线形阻尼谐振子完全相同的解。

## 5. 旋转平衡的稳定性

欧拉讨论了旋转平衡的概念。如果刚体绕固定轴旋转并受重力作用：

- **稳定平衡**：当质心在旋转轴正下方时（$\theta = 0$）。此时力矩 $M = -Mgd\sin\theta \approx -Mgd\theta$（小角度时为恢复力矩），微小的扰动导致小幅振荡。
- **不稳定平衡**：当质心在旋转轴正上方时（$\theta = \pi$）。令 $\theta = \pi + \delta$（$\delta$ 很小），则 $\sin\theta = \sin(\pi + \delta) = -\sin\delta \approx -\delta$，力矩 $M \approx Mgd\delta$（正反馈），微小的扰动导致大幅偏离。

> **批注**：旋转平衡的稳定性概念在工程中极为重要。桥梁、建筑物、机械的稳定性分析都涉及类似的概念。欧拉对稳定性问题的关注预示了后来整个稳定性理论的发展方向。在第421节的总结性注释中，欧拉指出所有绕固定轴旋转的问题都可以归结为计算几个关键的积分，其中最重要的 $\int rrdM$ 他赋予了专门的名称——"转动惯量"（moment of inertia），并宣布下一章将专门研究这个量。这标志着转动惯量作为独立物理量的正式确立。

---

## 本章小结

正文第4章将绕固定轴的旋转理论应用于具体问题：

1. **物理摆**：$\ddot{\theta} + (Mgd/I)\sin\theta = 0$
2. **等效单摆长度**：$\ell = I/(Md)$
3. **可逆性**：悬点与振动中心可以互换
4. **大角度摆动**：引出椭圆积分
5. **阻尼旋转**：旋转版本的阻尼谐振子
6. **稳定性**：稳定与不稳定平衡

> **批注**：到这一章为止，欧拉已经穷尽了绕固定轴旋转的所有主要问题。从下一章开始，他将面对一个根本性的挑战——当旋转轴不是固定的时候，会发生什么？这需要全新的数学工具（瞬时旋转轴、惯量张量、主轴、欧拉角），而正是这些工具的发明构成了欧拉最伟大的贡献。第4章的结果不仅完善了固定轴理论，更为第5章（转动惯量的系统研究）和第6章以后（自由旋转）的展开做了铺垫。

---

[上一章：正文第3章——旋转运动的产生](欧拉刚体运动理论_正文_第3章.md)

[下一章：正文第5章——旋转轴的确定](欧拉刚体运动理论_正文_第5章.md)
