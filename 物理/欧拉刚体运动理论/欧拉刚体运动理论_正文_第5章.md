# 正文第5章：旋转轴的确定

*Concerning the Moment of Inertia*

---

## 本章概述

这一章是全书最关键的章节之一。欧拉在这里系统地研究了**转动惯量**（moment of inertia）的概念，引入了**主轴**（principal axes）的定义，并推导了关于任意轴的转动惯量公式。这些结果为理解刚体的一般旋转运动提供了代数和几何基础。

> **批注**：到目前为止，欧拉在前面四章中处理了绕固定轴旋转的所有主要问题。本章转向一个新的方向：系统研究转动惯量的性质。在第422节（定义7）中，欧拉将转动惯量正式定义为"各元素乘以其到轴距离的平方之和"，并在第426节中解释了这个名称的由来——转动惯量在旋转运动中扮演的角色，与质量（惯性）在平移运动中的角色完全对称。

---

## 1. 转动惯量的定义

在第422节（定义7）中，欧拉给出了转动惯量的正式定义：

**定义7**：物体相对于某轴的转动惯量（moment of inertia），是所有乘积的总和——将各元素乘以其到轴距离的平方。

数学表达：
$$I = \sum_i m_i r_i^2$$

其中 $m_i$ 是第 $i$ 个质点的质量，$r_i$ 是该质点到旋转轴的垂直距离。

对于连续分布的刚体：
$$I = \int r^2\,dm = \int r^2\rho(\mathbf{r})\,dV$$

欧拉在第424节中指出，转动惯量总可以表示为物体质量乘以某个长度的平方。**推导**如下：令 $I = Mk^2$，则 $k = \sqrt{I/M}$。这个 $k$ 称为**回转半径**（radius of gyration），其物理意义是：如果把物体的全部质量集中到距离轴为 $k$ 的一个质点上，则该质点的转动惯量等于原物体的转动惯量。

即 $I = Mk^2$，在第425节中，欧拉给出核心公式 $d\omega = \frac{2Vf\,g\,dt}{Mk^2}$，将力矩 $Vf$、角速度变化 $d\omega$ 和转动惯量 $Mk^2$ 联系起来。

## 2. 平行轴定理

在第428节（问题25）中，欧拉证明了一个极其重要的定理——**平行轴定理**：

**已知物体关于轴 OA 的转动惯量，求同一物体关于另一平行轴 oa 的转动惯量。**

结果：
$$I_{oa} = I_{OA} + M \cdot gK^2 - M \cdot GK^2$$

特别地，如果 OA 通过质心 $G$（此时 $GK = 0$）：
$$\boxed{I_{oa} = I_{\text{cm}} + Md^2}$$

其中 $d$ 是两平行轴之间的距离。

**推导要点**：设 OA 过质心 $G$，oa 是平行轴，$G$ 到 oa 的距离为 $d$。在 oa 的坐标系中，第 $i$ 个质点的位置为 $\mathbf{r}_i' = \mathbf{r}_i - \mathbf{d}$（其中 $\mathbf{r}_i$ 是相对于 OA 的位置，$\mathbf{d}$ 是从 oa 到 OA 的位移矢量）。到 oa 的距离平方为：
$$r_{i,oa}^2 = |\boldsymbol{\rho}_i'|^2 = |\boldsymbol{\rho}_i - \mathbf{d}|^2$$

其中 $\boldsymbol{\rho}_i$ 是质点到 OA 的垂直矢量。展开：
$$r_{i,oa}^2 = |\boldsymbol{\rho}_i|^2 - 2\boldsymbol{\rho}_i \cdot \mathbf{d} + d^2$$

求和：
$$I_{oa} = \sum m_i|\boldsymbol{\rho}_i|^2 - 2\mathbf{d}\cdot\sum m_i\boldsymbol{\rho}_i + d^2\sum m_i$$

由于 OA 过质心，$\sum m_i\boldsymbol{\rho}_i = 0$（质心在 OA 上），因此中间项为零：
$$I_{oa} = I_{OA} + Md^2 = I_{\text{cm}} + Md^2$$

> **批注**：在第430节的推论中，欧拉得出了一个重要结论：在所有互相平行的轴中，过质心的那条轴对应的转动惯量最小。在第431节中，他进一步指出，任意平行轴的转动惯量等于过质心的平行轴的转动惯量加上 $Md^2$。这个定理的意义在于：只需知道过质心轴的转动惯量，就能立即求出所有平行轴的转动惯量。

## 3. 关于任意轴的转动惯量

在第433节（问题26）中，欧拉考虑了一个更一般的问题：已知物体的质量分布，求关于通过质心的任意轴的转动惯量。

设三个互相垂直的主轴为 IA、IB、IC，物体元素 dM 的坐标为 $(x, y, z)$。令：
$$A = \int x^2 dM, \quad B = \int y^2 dM, \quad C = \int z^2 dM$$
$$D = \int xy\,dM, \quad E = \int xz\,dM, \quad F = \int yz\,dM$$

对于与 IA 夹角为 $\zeta$、与 AIB 面夹角为 $\eta$ 的任意轴 IG，转动惯量为：
$$I_{IG} = A(\sin^2\zeta + \cos^2\zeta\sin^2\eta) + B(\cos^2\zeta + \sin^2\zeta\sin^2\eta) + C\cos^2\eta - 2D\sin\zeta\cos\zeta\cos^2\eta - 2E\cos\zeta\sin\eta\cos\eta - 2F\sin\zeta\sin\eta\cos\eta$$

**推导思路**：第 $i$ 个质点（坐标 $(x_i, y_i, z_i)$）到方向为 $\hat{\mathbf{n}}$ 的轴的垂直距离平方为 $r_i^2 = |\mathbf{r}_i|^2 - (\mathbf{r}_i \cdot \hat{\mathbf{n}})^2$。将 $\hat{\mathbf{n}}$ 用角度 $\zeta$、$\eta$ 表示的方向余弦代入，展开后利用 $A, B, C, D, E, F$ 的定义，合并同类项即得上式。

> **批注**：这个公式是欧拉对惯量理论最核心的贡献之一。它表明转动惯量关于轴的方向是一个二次型——这正是后来"惯量张量"（inertia tensor）概念的来源。$A$、$B$、$C$ 总是正数，而 $D$、$E$、$F$ 可正可负，取决于物体的质量分布。

## 4. 主轴的发现

在第438-442节中，欧拉证明了一个划时代的结果——**每个物体都存在三个互相垂直的主轴**，关于这些轴的转动惯量取极值（极大或极小）。

**定义8**（第446节）：**物体的主轴**（principal axes）是通过质心的三条轴，关于它们的转动惯量取极大或极小值。

**关键定理**（第443节）：这三个主轴互相垂直。

**证明要点**：转动惯量作为轴方向的函数 $I(\hat{\mathbf{n}})$ 在单位球面上连续。由极值理论，$I$ 在约束 $\alpha^2+\beta^2+\gamma^2=1$ 下的极值条件为：

$$\frac{\partial}{\partial\alpha}(I - \lambda(\alpha^2+\beta^2+\gamma^2-1)) = 0$$

这给出特征值问题 $\mathbf{I}\hat{\mathbf{n}} = \lambda\hat{\mathbf{n}}$，其中 $\mathbf{I}$ 是对称的惯量矩阵。由于 $\mathbf{I}$ 是实对称矩阵，它有三个实特征值和三个互相正交的特征向量——这就是三个主轴。

当选择主轴为坐标轴时，交叉项消失（因为 $\hat{\mathbf{n}}$ 是特征向量，惯量积为零）：
$$D = \int xy\,dM = 0, \quad E = \int xz\,dM = 0, \quad F = \int yz\,dM = 0$$

关于任意轴 IG 的转动惯量简化为：
$$\boxed{I_{IG} = Maa\cos^2\alpha + Mbb\cos^2\beta + Mcc\cos^2\gamma}$$

其中 $Maa$、$Mbb$、$Mcc$ 是关于三个主轴的转动惯量，$\alpha$、$\beta$、$\gamma$ 是 IG 与三个主轴的夹角，满足 $\cos^2\alpha + \cos^2\beta + \cos^2\gamma = 1$。

> **批注**：主轴的发现是欧拉对刚体力学最深刻的贡献之一。它表明，虽然一个物体有无穷多条轴，每条轴对应一个转动惯量，但所有这些信息都可以从三个主转动惯量中完全恢复。这在数学上等价于将一个对称矩阵对角化——后来的线性代数将这一过程一般化。欧拉在第438节中通过求解一个三次方程来找到主轴，在第442节中证明了一个已知主轴可以容易地推出另外两个，体现了极高的数学技巧。

## 5. 转动惯量的几何表示

在第457节的注释中，欧拉用一个优美的球面几何图像来表示转动惯量。

以质心 $I$ 为球心作一球面，三个主轴与球面的交点为 $A$、$B$、$C$（构成三个象限）。对球面上任意一点 $S$（代表一条轴的方向），关于该轴的转动惯量为：
$$I_{IS} = Maa\cos^2 AS + Mbb\cos^2 BS + Mcc\cos^2 CS$$

这是主轴公式的直接应用，其中 $\cos AS$、$\cos BS$、$\cos CS$ 就是轴 IS 与三个主轴的方向余弦。

若 $aa > bb > cc$，则：
- $A$ 点对应最大转动惯量（$I = Maa$，因为 $\cos AS = 1$，其余为零）
- $C$ 点对应最小转动惯量（$I = Mcc$）
- $B$ 点对应中间值（$I = Mbb$）

> **批注**：这种几何表示法将转动惯量的分布可视化——球面上每一点的颜色代表转动惯量的大小。欧拉在第458-461节中进一步研究了转动惯量相等点的轨迹：当 $I = Mbb$ 时，这些点位于通过 $B$ 和 $E$ 的大圆上；当 $I > Mbb$ 时，轨迹是以 $A$ 为中心的球面椭圆；当 $I < Mbb$ 时，轨迹是以 $C$ 为中心的球面椭圆。这些结果后来成为椭球惯量面（ellipsoid of inertia）理论的基础。

## 6. 复合体的转动惯量

在第464节（问题31）中，欧拉证明了一个实用的定理：复合体的转动惯量可以从各部分的转动惯量组合而得。

设物体由质量为 $M$（转动惯量 $Mmm$）和质量为 $N$（转动惯量 $Nnn$）的两部分组成，各部分质心间距为 $c$，则整个物体关于通过其质心的平行轴的转动惯量为：
$$I = Mmm + Nnn + \frac{MN c^2 \sin^2\theta}{M+N}$$

其中 $\theta$ 是质心连线与轴之间的夹角。

**推导思路**：复合体的质心位于两部分质心的连线上，距第一部分质心为 $\frac{Nc}{M+N}$，距第二部分为 $\frac{Mc}{M+N}$。对每一部分分别应用平行轴定理（将各部分的惯量从各自的质心平移到复合体质心），然后相加：
$$I = \left[Mmm + M\left(\frac{Nc}{M+N}\right)^2\sin^2\theta\right] + \left[Nnn + N\left(\frac{Mc}{M+N}\right)^2\sin^2\theta\right]$$
$$= Mmm + Nnn + \frac{MN(M+N)c^2\sin^2\theta}{(M+N)^2} = Mmm + Nnn + \frac{MN c^2\sin^2\theta}{M+N}$$

> **批注**：这个定理使得复杂形状物体的转动惯量可以通过将它们分解为简单部分来计算——正如平行轴定理将平行轴的计算简化一样。在第469节中，欧拉将此推广到更多部分的组合。这一方法至今仍是工程力学中计算复杂物体转动惯量的标准做法。

---

## 本章小结

正文第5章建立了转动惯量的系统理论：

1. **转动惯量的定义**：$I = \int r^2 dm$（第422节）
2. **平行轴定理**：$I = I_{\text{cm}} + Md^2$（第428节）
3. **任意轴的转动惯量**：关于方向角的二次型公式（第433节）
4. **主轴定理**：每个物体有三个互相垂直的主轴（第438-444节）
5. **主转动惯量公式**：$I = Maa\cos^2\alpha + Mbb\cos^2\beta + Mcc\cos^2\gamma$（第452节）
6. **复合体的转动惯量**：从各部分的转动惯量组合而得（第464节）

> **批注**：第5章是整本书的转折点。在此之前（第1-4章），欧拉只处理了绕固定轴的旋转。从本章开始，他引入了描述任意方向旋转所需的全部数学工具。主轴概念的发现尤其重要——它将一个看起来需要无穷多个参数来描述的问题（每个方向一个转动惯量），简化为只需三个参数（三个主转动惯量）。这种"用最少的参数完全描述系统的惯性性质"的思想，后来成为分析力学的核心范式。在第470节的末尾，欧拉预告了后续内容：从最简单的几何形状（细线、薄板）开始，逐步计算各种常见物体的主转动惯量。

---

[上一章：正文第4章——受任意力作用下的旋转](欧拉刚体运动理论_正文_第4章.md)

[下一章：正文第6章——各种物体的转动惯量](欧拉刚体运动理论_正文_第6章.md)
