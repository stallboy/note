# 正文第1章：刚体的平移运动

*Concerning the Progressive Motion of Rigid Bodies*

---

## 本章概述

从这一章开始，欧拉进入全书的核心——刚体运动理论。第1章奠定了刚体力学的基石：**刚体的严格定义**和**质心运动定理**。

> **批注**：这是整个刚体力学大厦的第一块砖。欧拉在这里做了两件开创性的事情：第一，给出了刚体的严格数学定义（定义1，约第260节）；第二，证明了刚体的平移运动完全由质心的运动决定。这两个结果看似简单，但它们的严格建立需要极其精细的推理。值得注意的是，欧拉的刚体定义与牛顿《自然哲学的数学原理》中对"刚性"的隐含假设不同——牛顿从未明确定义过刚体，而欧拉将其作为第一条定义提出，体现了他从物理直觉向数学严格性的转变。Ian Bruce在译注中也指出，欧拉的定义（第260节）实质上是用约束条件"各元素间距离不变"来刻画刚体，而非诉诸"不可变形"这种形而上学的说法。

---

## 定义1：刚体（rigid body）

> "A body is called rigid, the shape of which undergoes no change, or the individual elements of which maintain a constant distance amongst themselves."

欧拉的刚体定义意味着：在刚体的运动过程中，任意两个质点之间的距离保持不变。如果第 $i$ 个质点的位置为 $\mathbf{r}_i$，第 $j$ 个质点的位置为 $\mathbf{r}_j$，则：
$$|\mathbf{r}_i - \mathbf{r}_j| = \text{常数}$$

> **批注**：这个定义的深刻之处在于，它用纯数学语言（距离不变）定义了一个物理概念（刚性）。这种"用约束条件定义物理对象"的方法是分析力学的基本范式。刚体是最简单的非质点力学系统——它有无穷多个质点，但距离约束将自由度从无穷多个减少到了6个（3个平动 + 3个转动）。欧拉在第263节的解释（Explicatio）中特别强调，他并非要求刚体"绝对不可变形"——世界上不存在这样的物体——而是说在所受力的作用下不发生可察觉的形变即可。这一实用主义的定义方法，与后来柯西（Cauchy）的弹性力学理论形成了有趣的对比：柯西正是从欧拉放弃的地方出发，研究当"刚性"条件被放松后会发生什么。

## 定义2：质心（centre of gravity or centre of inertia）

欧拉定义了质心——质量分布的"平均位置"：

$$\mathbf{R}_{\text{cm}} = \frac{\sum_i m_i \mathbf{r}_i}{\sum_i m_i} = \frac{1}{M}\sum_i m_i \mathbf{r}_i$$

对于连续分布的刚体：
$$\mathbf{R}_{\text{cm}} = \frac{1}{M}\int \mathbf{r}\,dm = \frac{1}{M}\int \mathbf{r}\rho(\mathbf{r})\,dV$$

其中 $M = \sum_i m_i$ 是总质量，$\rho$ 是密度。

> **批注**：欧拉是第一个系统使用质心概念的人之一。他认识到，对于刚体的整体运动，质心起着"代表"的作用——好像刚体的全部质量都集中在质心上一样。这个认识在下面的质心运动定理中得到了严格的表述。值得注意的是，欧拉在第285-286节中刻意区分了"质心"（centre of inertia）和"重心"（centre of gravity）——他认为重力是一种外在力，不应将一个仅依赖于物体质量分布的几何点与重力联系起来。这一思想在当时是非常超前的，因为18世纪的大多数力学家仍将"重心"和"质心"混为一谈。

## 定义3：平移运动（progressive motion）

欧拉定义了平移运动：刚体上所有点具有相同的速度。

$$\mathbf{v}_1 = \mathbf{v}_2 = \cdots = \mathbf{v}_i = \cdots = \mathbf{V}_{\text{cm}}$$

在平移运动中，刚体不旋转，只做整体移动。

> **批注**：欧拉对平移运动的定义是精确的——不是"看起来像是平移"，而是"每个点都有相同的速度"。这个精确的定义使得平移运动可以与旋转运动严格区分开来。在第269节的注释（Scholium）中，欧拉指出天体运动通常同时包含平移和旋转（他特别提到月球总以同一面朝向地球，因此不是纯平移运动），这表明他已经清楚地认识到天体运动的复杂性。

## 命题1：质心运动定理

**刚体质心的运动，如同将刚体的全部质量集中于质心，并将所有外力集中于该点一样。**

数学表述：设外力的总和为 $\mathbf{F}_{\text{外}} = \sum_i \mathbf{F}_i^{\text{外}}$，则：
$$M\frac{d^2\mathbf{R}_{\text{cm}}}{dt^2} = \mathbf{F}_{\text{外}}$$

### 证明

欧拉的证明如下：

对刚体中的第 $i$ 个质点，运动方程为：
$$m_i\frac{d^2\mathbf{r}_i}{dt^2} = \mathbf{F}_i^{\text{外}} + \sum_{j \neq i} \mathbf{F}_{ij}$$

其中 $\mathbf{F}_i^{\text{外}}$ 是外力，$\mathbf{F}_{ij}$ 是第 $j$ 个质点对第 $i$ 个质点的内力。

对所有质点求和：
$$\sum_i m_i\frac{d^2\mathbf{r}_i}{dt^2} = \sum_i \mathbf{F}_i^{\text{外}} + \sum_i\sum_{j \neq i} \mathbf{F}_{ij}$$

由牛顿第三定律，$\mathbf{F}_{ij} = -\mathbf{F}_{ji}$，所以所有内力成对抵消：
$$\sum_i\sum_{j \neq i} \mathbf{F}_{ij} = 0$$

下面详细说明内力成对抵消的过程。将双重求和展开，每一对质点 $(i, j)$（其中 $i \neq j$）在求和中出现两次：一次是 $\mathbf{F}_{ij}$（$j$ 对 $i$ 的力），另一次是 $\mathbf{F}_{ji}$（$i$ 对 $j$ 的力）。由牛顿第三定律 $\mathbf{F}_{ij} = -\mathbf{F}_{ji}$，这两项恰好相消。由于所有 $(i, j)$ 对都如此，整个双重求和为零。

因此：
$$\sum_i m_i\frac{d^2\mathbf{r}_i}{dt^2} = \sum_i \mathbf{F}_i^{\text{外}}$$

左边可以写成：
$$\sum_i m_i\frac{d^2\mathbf{r}_i}{dt^2} = \frac{d^2}{dt^2}\left(\sum_i m_i\mathbf{r}_i\right)$$

这里将求和号与微分号交换，是因为求和是有限的（或积分与微分交换由一致收敛性保证）。根据质心定义 $\mathbf{R}_{\text{cm}} = \frac{1}{M}\sum_i m_i\mathbf{r}_i$，有 $\sum_i m_i\mathbf{r}_i = M\mathbf{R}_{\text{cm}}$，代入得：

$$\frac{d^2}{dt^2}\left(\sum_i m_i\mathbf{r}_i\right) = \frac{d^2}{dt^2}\left(M\mathbf{R}_{\text{cm}}\right) = M\frac{d^2\mathbf{R}_{\text{cm}}}{dt^2}$$

其中 $M$ 是常数（刚体总质量不变），因此可以提到微分号外面。

右边即为所有外力的矢量和：
$$\sum_i \mathbf{F}_i^{\text{外}} = \mathbf{F}_{\text{外}}$$

最终得到：
$$M\frac{d^2\mathbf{R}_{\text{cm}}}{dt^2} = \mathbf{F}_{\text{外}}$$

证毕。$\blacksquare$

> **批注**：这个证明的精髓在于内力的成对抵消。由于内力满足牛顿第三定律（作用等于反作用），它们对整个系统的总效果为零。因此，刚体的整体运动（质心运动）只取决于外力。这个定理的一个直接推论是：**内力不能改变刚体的质心运动状态**。无论刚体内部发生什么（只要保持刚性），质心只对外力有响应。欧拉在第296-297节中引入了"基本力"（elementary forces）的概念——这些力作用在每个元素上，产生与实际运动相同的状态变化。这一概念使得他能够精确计算刚体内部结构需要承受的力，是刚体力学中连接运动学与静力学的关键桥梁。

## 推论1：孤立刚体的运动

如果刚体不受任何外力（$\mathbf{F}_{\text{外}} = 0$），则：
$$M\frac{d^2\mathbf{R}_{\text{cm}}}{dt^2} = 0$$

由此可以直接积分。由于 $M$ 是常数，两边除以 $M$：
$$\frac{d^2\mathbf{R}_{\text{cm}}}{dt^2} = 0$$

对时间积分一次：
$$\frac{d\mathbf{R}_{\text{cm}}}{dt} = \mathbf{C}_1$$

其中 $\mathbf{C}_1$ 是积分常数矢量。设初始时刻质心速度为 $\mathbf{V}_0$，则 $\mathbf{C}_1 = \mathbf{V}_0$。再积分一次：
$$\mathbf{R}_{\text{cm}}(t) = \mathbf{V}_0 t + \mathbf{R}_0$$

即质心以恒定速度 $\mathbf{V}_0$ 做匀速直线运动。

**质心以恒定速度做匀速直线运动**——这是惯性原理对刚体的推广。

> **批注**：这个推论意味着，如果我们把太阳系看作一个不受外力的"刚体"（近似），则太阳系的质心在做匀速直线运动。同样，如果我们踢一个足球，足球的质心沿着一条抛物线运动（受重力作用），无论足球同时如何旋转。这正是欧拉在第269节中提到的天文学背景——将行星视为质点处理平移运动，再叠加旋转运动。

## 推论2：力的简化

质心运动定理意味着，对于平移运动的讨论，我们不需要知道每个力的具体作用点——只需要知道所有外力的**矢量和**。

$$\mathbf{F}_{\text{合}} = \sum_i \mathbf{F}_i^{\text{外}}$$

这个合力可以"平移"到质心上，用来计算质心的加速度。

> **批注**：但欧拉立即警告读者——力的简化只对平移运动有效。对于旋转运动，力的作用点至关重要。同一个力作用在不同的点上，对平移运动的效果相同，但对旋转运动的效果可能完全不同。这正是"力矩"概念的来源——它度量了力产生旋转效果的能力。欧拉在第289节明确指出，力的等效替代（用一个合力代替分布力系）只有对刚性成立，对非刚性体则不然——这预示了弹性力学与刚体力学的基本分野。

## 命题2：动量定理

刚体的总动量等于质心动量：
$$\mathbf{P} = \sum_i m_i\mathbf{v}_i = M\mathbf{V}_{\text{cm}}$$

**推导**：由质心定义 $\mathbf{R}_{\text{cm}} = \frac{1}{M}\sum_i m_i\mathbf{r}_i$，两边对时间求导：
$$\mathbf{V}_{\text{cm}} = \frac{d\mathbf{R}_{\text{cm}}}{dt} = \frac{1}{M}\sum_i m_i\frac{d\mathbf{r}_i}{dt} = \frac{1}{M}\sum_i m_i\mathbf{v}_i$$

因此 $\sum_i m_i\mathbf{v}_i = M\mathbf{V}_{\text{cm}}$。

因此，动量定理可以表述为：
$$\frac{d\mathbf{P}}{dt} = \mathbf{F}_{\text{外}}$$

**推导**：对 $\mathbf{P} = M\mathbf{V}_{\text{cm}}$ 求时间导数：
$$\frac{d\mathbf{P}}{dt} = M\frac{d\mathbf{V}_{\text{cm}}}{dt} = M\frac{d^2\mathbf{R}_{\text{cm}}}{dt^2} = \mathbf{F}_{\text{外}}$$

最后一步利用了质心运动定理 $M\ddot{\mathbf{R}}_{\text{cm}} = \mathbf{F}_{\text{外}}$。

> **批注**：这个定理将质点系的动量定理应用于刚体，是质心运动定理的等价表述。欧拉在第290-294节中将此定理系统化，表明整个刚体的平移运动可以完全还原为质心的运动——这就是为什么天体力学中可以将行星视为质点的原因。

## 命题3：动能与质心动能的关系（柯尼希定理的雏形）

欧拉指出，刚体中每个质点的速度可以分解为质心速度和相对于质心的速度：
$$\mathbf{v}_i = \mathbf{V}_{\text{cm}} + \mathbf{v}_i'$$

其中 $\mathbf{v}_i' = \mathbf{v}_i - \mathbf{V}_{\text{cm}}$ 是相对于质心的速度。

总动能为：
$$T = \sum_i \frac{1}{2}m_i v_i^2 = \sum_i \frac{1}{2}m_i(\mathbf{V}_{\text{cm}} + \mathbf{v}_i')^2$$

展开被积函数：
$$(\mathbf{V}_{\text{cm}} + \mathbf{v}_i')^2 = (\mathbf{V}_{\text{cm}} + \mathbf{v}_i') \cdot (\mathbf{V}_{\text{cm}} + \mathbf{v}_i') = V_{\text{cm}}^2 + 2\mathbf{V}_{\text{cm}} \cdot \mathbf{v}_i' + v_i'^2$$

因此：
$$T = \sum_i \frac{1}{2}m_i V_{\text{cm}}^2 + \sum_i m_i \mathbf{V}_{\text{cm}} \cdot \mathbf{v}_i' + \sum_i \frac{1}{2}m_i v_i'^2$$
$$= \frac{1}{2}MV_{\text{cm}}^2 + \mathbf{V}_{\text{cm}} \cdot \sum_i m_i\mathbf{v}_i' + \sum_i \frac{1}{2}m_i v_i'^2$$

中间项的计算如下：由于 $\mathbf{v}_i' = \mathbf{v}_i - \mathbf{V}_{\text{cm}}$，有：
$$\sum_i m_i\mathbf{v}_i' = \sum_i m_i(\mathbf{v}_i - \mathbf{V}_{\text{cm}}) = \sum_i m_i\mathbf{v}_i - M\mathbf{V}_{\text{cm}}$$

由命题2的结果 $\sum_i m_i\mathbf{v}_i = M\mathbf{V}_{\text{cm}}$，代入得：
$$\sum_i m_i\mathbf{v}_i' = M\mathbf{V}_{\text{cm}} - M\mathbf{V}_{\text{cm}} = 0$$

因此中间项为零（在质心参考系中，总动量为零）。最终：

$$\boxed{T = \frac{1}{2}MV_{\text{cm}}^2 + \sum_i \frac{1}{2}m_i v_i'^2}$$

**总动能 = 质心动能 + 相对于质心的动能**

> **批注**：这就是著名的柯尼希定理（König's theorem）。它表明刚体的动能可以自然地分解为两部分：质心平动的动能和绕质心运动的动能。这个分解是质心-转动分解定理在能量层面的体现。右边第二项（相对于质心的动能）在纯平移时为零——这对应于所有质点速度相同的情况。在后面引入转动惯量和角速度后，这一项将变为 $\frac{1}{2}I\omega^2$ 或更一般的 $\frac{1}{2}\boldsymbol{\omega} \cdot \mathbf{I} \cdot \boldsymbol{\omega}$。欧拉本人并未以现代形式陈述柯尼希定理，但他在第300-304节中对"基本力"（elementary forces）的分析实质上已经蕴含了动能分解的思想。

## 注释（Scholium）：为什么刚体是六自由度系统

欧拉在第264节的注释中，从几何直觉出发逐步论证了刚体运动的自由度。

### 第一步：固定一个点

想象刚体中有一个点 $A$ 被固定。其余的每个点只能在以 $A$ 为球心的球面上运动——因为它们到 $A$ 的距离不能改变。取第二个点 $B$，它在以 $A$ 为球心的球面上运动，需要 **2个角参数**（如纬度和经度）来确定位置。

### 第二步：固定两个点

现在点 $A$ 和点 $B$ 的位置都确定了。取第三个点 $C$，它必须同时满足：
- 到 $A$ 的距离不变 → $C$ 在以 $A$ 为球心的球面上
- 到 $B$ 的距离不变 → $C$ 在以 $B$ 为球心的球面上

两个球面的交线是一个圆。因此 $C$ 只能在某个圆上运动，只需 **1个角参数** 来确定其位置。

### 第三步：三个点确定整个刚体

一旦三个不共线的点 $A$、$B$、$C$ 的位置都确定了，刚体中其余每个点的位置也随之确定——因为每个点到 $A$、$B$、$C$ 的距离都是固定的（§261-262）。因此，三个不共线点的位置完全确定了整个刚体的位置。

### 汇总

- 点 $A$ 在空间中的位置：**3个自由度**（三个坐标 $x_A, y_A, z_A$）
- 点 $B$ 相对于 $A$ 的位置（在球面上）：**2个自由度**
- 点 $C$ 相对于 $A$ 和 $B$ 的位置（在圆上）：**1个自由度**

总共 **6个自由度**。

> **欧拉原文**（§264）："一个刚体能接受无穷多种运动。若其中一个点被固定，另一个点可以在球面上运动；不论第二个点如何运动，第三个点可以或快或慢地运动，只要它保持与前两个点的适当距离。由此可知，若没有任何点被固定，可能的运动将更为复杂。但只要三个不共线点的运动已知，所有其余点的运动——即整个物体的运动——也就确定了。"

用另一种等价的视角看：3个不共线点确定整个刚体，3个点共有 $3 \times 3 = 9$ 个坐标，但3对距离保持不变提供3个约束，因此 $9 - 3 = 6$ 个独立变量。

> **批注**：这个"6个自由度"的结论是刚体力学的出发点。欧拉的论证方法值得注意——他不是从抽象的约束计数出发，而是从几何直觉逐步构建：先固定一个点（球的中心），再固定第二个点（球面上的位置），再固定第三个点（圆上的位置）。每一步都增加一个直观的自由度。在第308节的末尾，欧拉明确了他的全书规划：先研究绕固定轴的旋转（两个点固定），再推广到可移动的轴，最后达到完全自由的一般运动——这一由简到繁的策略正是分析力学的典型方法。他还指出，如果在纯平移运动之外加上旋转运动，"运动就变成了另一种类型"——这预示了后面18章的核心问题：如何处理平动与转动的耦合。

---

## 本章小结

正文第1章建立了刚体力学的两大基石：

1. **刚体的严格定义**：各部分间距离不变的物体
2. **质心运动定理**：$M\ddot{\mathbf{R}}_{\text{cm}} = \mathbf{F}_{\text{外}}$，质心运动等同于质点运动
3. **动能分解**：$T = \frac{1}{2}MV_{\text{cm}}^2 + T_{\text{相对}}$
4. **刚体有6个自由度**：3个平动 + 3个转动

> **批注**：第1章解决的是刚体6个自由度中的3个——平动自由度。质心运动定理告诉我们，刚体的平动完全由外力的矢量和决定，与刚体的形状和取向无关。剩下的问题是：刚体的转动（另外3个自由度）由什么决定？这就是后面18章要回答的问题。纵观本章，欧拉的贡献不仅在于具体定理的证明，更在于他建立了一套处理连续体的方法论：用微积分的语言（积分 $\int$）处理分布量（质量、力），用"等效"的概念将分布力系归结为一个合力。这套方法论直接影响了后来的拉格朗日力学和哈密顿力学。

---

[上一章：引言第6章——阻尼介质中的曲线运动](欧拉刚体运动理论_引言_第6章.md)

[下一章：正文第2章——绕固定轴的无扰动旋转](欧拉刚体运动理论_正文_第2章.md)
