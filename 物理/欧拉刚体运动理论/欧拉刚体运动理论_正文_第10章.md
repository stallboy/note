# 正文第10章：受任意力作用的刚体自由运动

*Concerning the Free Motion of Rigid Bodies Acted on by Any Forces*

---

## 本章概述

这是全书的**巅峰之章**。在这里，欧拉将前面所有成果融为一体，推导出了经典的**欧拉方程**（Euler's equations）——描述刚体在外力矩作用下旋转运动的完整理论。这一章对应Ian Bruce英译本卷二第15章"Concerning the Free Motion of Rigid Bodies Acted on by Any Forces"。

> **批注**：欧拉在注解789中特别指出，前面的推导虽然正确，但路径曲折，而本章将用"单一原理"重新建立整个理论。这种"从基本原理出发，逐步构建"的风格，正是欧拉科学方法的精髓。值得注意的是，欧拉在注解790中采用了一种"逆方法"——先问需要什么样的力才能产生给定的运动变化，然后再反过来求解——这种思路在变分法中有着深远的类比。

---

## 1. 运动的四个要素（定理10）

欧拉在定理10中指出：无论刚体受何种力的作用，瞬时变化包含在四个效应中：

1. **质心速度的变化**
2. **质心方向的变化**
3. **绕通过质心的轴旋转的角速度变化**
4. **旋转轴本身的变化**

> **批注**：这四个要素揭示了刚体动力学的完整图景——前两个对应平动（progressive motion），后两个对应转动（rotational motion）。欧拉在推论788中指出，整个刚体运动理论的基石就是：给定任意力，能够确定这四个变化量。这个框架至今仍是刚体动力学的基本范式。

## 2. 角速度的分量表达（问题85）

欧拉在问题85中解决了：给定绕任意轴 IO（通过质心）以角速度 $\omega'$ 旋转的刚体，求各元素的速度沿三个主轴方向的分量。

设旋转轴 IO 与三个主轴 IA, IB, IC 的夹角分别为 $\alpha, \beta, \gamma$，则角速度矢量为：
$$\boldsymbol{\omega} = \omega'(\cos\alpha\,\hat{\mathbf{e}}_A + \cos\beta\,\hat{\mathbf{e}}_B + \cos\gamma\,\hat{\mathbf{e}}_C)$$

各元素的速度由 $\mathbf{v} = \boldsymbol{\omega} \times \mathbf{r}$ 给出。设元素坐标为 $(x, y, z)$（沿三个主轴），则：
$$\mathbf{v} = \boldsymbol{\omega} \times \mathbf{r} = \omega'(\cos\alpha\,\hat{\mathbf{e}}_A + \cos\beta\,\hat{\mathbf{e}}_B + \cos\gamma\,\hat{\mathbf{e}}_C) \times (x\,\hat{\mathbf{e}}_A + y\,\hat{\mathbf{e}}_B + z\,\hat{\mathbf{e}}_C)$$

展开叉积（利用 $\hat{\mathbf{e}}_A \times \hat{\mathbf{e}}_B = \hat{\mathbf{e}}_C$ 等）：
$$v_A = \omega'(z\cos\beta - y\cos\gamma)$$
$$v_B = \omega'(x\cos\gamma - z\cos\alpha)$$
$$v_C = \omega'(y\cos\alpha - x\cos\beta)$$

> **批注**：这是欧拉首次在球面坐标下系统推导速度的分量表达式。Ian Bruce在注释中指出，这实际上是**叉积（cross product / vector product）的首次出现**——欧拉将角速度的分量表示为沿各轴的投影，然后用它们来表达速度分量。点积（dot product）已在前面第10章问题68中使用。因此，向量乘法的两种基本形式——点积和叉积——都是由欧拉在1765年建立的。这是一个被低估的数学贡献。

## 3. 欧拉方程的推导（问题85-88）

欧拉通过四个问题逐步推导出最终的方程。

### 问题86：给定旋转变化，求所需的力

给定绕轴 IO 旋转的角速度 $\omega'$ 及其变化 $d\omega'$，$d\alpha$，$d\beta$，$d\gamma$，求作用在各元素上所需的力。

欧拉首先写出速度分量（来自问题85），然后求微分，得到加速度分量，最后乘以 $dM/(2g\,dt)$ 得到力。

**详细推导**：第 $i$ 个元素的速度分量为（用简记 $c_\alpha = \cos\alpha$ 等）：
$$v_A = \omega'(zc_\beta - yc_\gamma), \quad v_B = \omega'(xc_\gamma - zc_\alpha), \quad v_C = \omega'(yc_\alpha - xc_\beta)$$

对时间求微分，考虑 $\omega'$、$\alpha$、$\beta$、$\gamma$ 都在变化：
$$dv_A = d\omega'(zc_\beta - yc_\gamma) + \omega'(-z\,d\alpha\,s_\beta + y\,d\alpha\,s_\gamma) + \omega'(dz\,c_\beta - dy\,c_\gamma)$$

类似地得到 $dv_B$ 和 $dv_C$。其中 $dz = v_C\,dt$，$dy = v_B\,dt$ 等是由元素运动引起的坐标变化。

### 问题87：关于三个主轴的力矩

欧拉将各元素上的力关于三个主轴取矩并求和。

**推导步骤**：第 $i$ 个元素的力为 $\mathbf{f}_i = \frac{dM}{2g\,dt}\frac{d\mathbf{v}_i}{dt}$。关于轴 IA 的力矩为 $M_A = \sum (yf_C - zf_B)\,dM$，其中 $f_B$、$f_C$ 是力的 $B$、$C$ 分量。

将 $dv_B$ 和 $dv_C$ 的表达式代入，并在主轴坐标系中利用 $\sum xy\,dM = 0$，$\sum xz\,dM = 0$，$\sum yz\,dM = 0$（惯量积为零），大量交叉项消去。

**交叉项消去的关键**：例如，含有 $\sum xyz\,dM$ 的项在求和后可以分解为 $\sum x \cdot yz\,dM$。在主轴坐标系中，$\sum yz\,dM = F = 0$，因此 $\sum xyz\,dM = \sum x \cdot 0 = 0$。类似地，其他交叉项也因惯量积为零而消去。

最终得到关于轴 IA 的力矩：
$$M_A = \frac{M}{2g\,dt}\left(aa\,d\omega'\cos\alpha - \omega'aa\,d\alpha\sin\alpha + \omega'^2(cc-bb)\,dt\cos\beta\cos\gamma\right)$$

关于轴 IB 和 IC 有类似表达式。

### 问题88：欧拉方程

设关于三个主轴的外力矩分别为 $P$, $Q$, $R$，主转动惯量为 $Maa$, $Mbb$, $Mcc$。

令问题87中计算的力矩等于外力矩，得到：

**关于轴 IA**：
$$\frac{2gP\,dt}{Maa} = d(\omega'\cos\alpha) - \omega'\,d\alpha\sin\alpha + \frac{cc-bb}{aa}\omega'^2\,dt\cos\beta\cos\gamma$$

**推导**：力矩平衡给出 $M_A = P$。将 $M_A$ 的表达式除以 $\frac{Maa}{2g\,dt}$：
$$\frac{2gP\,dt}{Maa} = d\omega'\cos\alpha - \omega'\sin\alpha\,d\alpha + \frac{cc-bb}{aa}\omega'^2\cos\beta\cos\gamma\,dt$$

注意 $d(\omega'\cos\alpha) = d\omega'\cos\alpha - \omega'\sin\alpha\,d\alpha$，因此：
$$\frac{2gP\,dt}{Maa} = d(\omega'\cos\alpha) + \frac{cc-bb}{aa}\omega'^2\cos\beta\cos\gamma\,dt$$

**关于轴 IB**：
$$\frac{2gQ\,dt}{Mbb} = d(\omega'\cos\beta) - \omega'\,d\beta\sin\beta + \frac{aa-cc}{bb}\omega'^2\,dt\cos\gamma\cos\alpha$$

**关于轴 IC**：
$$\frac{2gR\,dt}{Mcc} = d(\omega'\cos\gamma) - \omega'\,d\gamma\sin\gamma + \frac{bb-aa}{cc}\omega'^2\,dt\cos\alpha\cos\beta$$

> **批注**：Ian Bruce在翻译问题88时特别将欧拉的结果与现代教科书中的形式进行了对比。例如，Synge & Griffith 的《Principles of Mechanics》（1942）中给出的现代形式为：
> $$A\dot{\omega}_1 - (B-C)\omega_2\omega_3 = G_1$$
> $$B\dot{\omega}_2 - (C-A)\omega_3\omega_1 = G_2$$
> $$C\dot{\omega}_3 - (A-B)\omega_1\omega_2 = G_3$$
>
> 其中 $A$, $B$, $C$ 是主转动惯量，$G_1$, $G_2$, $G_3$ 是外力矩。这与欧拉原式完全等价。

## 4. 欧拉方程的简化形式（注解808）

欧拉在注解808中给出了一个极其优美的简化。设：

$$x = \omega'\cos\alpha, \quad y = \omega'\cos\beta, \quad z = \omega'\cos\gamma$$

这三个量分别是角速度矢量在三个主轴方向上的分量。

**推导简化过程**：以第一个方程为例。将 $x = \omega'\cos\alpha$ 代入：
$$dx = d(\omega'\cos\alpha)$$

而 $\omega'^2\cos\beta\cos\gamma = \frac{(\omega'\cos\beta)(\omega'\cos\gamma) \cdot \omega'}{\omega'} = \frac{yz \cdot \omega'}{\omega'} = yz$ ... 不完全正确。实际上：

$$\omega'^2\cos\beta\cos\gamma = \omega' \cdot \omega'\cos\beta \cdot \cos\gamma = \omega' \cdot y \cdot \frac{z}{\omega'\cos\beta} \cdot \cos\gamma$$

更直接地，注意 $\omega'^2 = x^2 + y^2 + z^2$（因为 $\cos^2\alpha + \cos^2\beta + \cos^2\gamma = 1$），而 $\omega'\cos\beta = y$，$\omega'\cos\gamma = z$，因此：
$$\omega'^2\cos\beta\cos\gamma = \omega' \cdot y \cdot \frac{z}{\omega'} = yz$$

这不是直接的。正确的做法是：从 $y = \omega'\cos\beta$ 和 $z = \omega'\cos\gamma$，有 $\cos\beta = y/\omega'$ 和 $\cos\gamma = z/\omega'$。因此：
$$\omega'^2\cos\beta\cos\gamma = \omega'^2 \cdot \frac{y}{\omega'} \cdot \frac{z}{\omega'} = yz$$

代入第一个方程，除以 $dt$：
$$\frac{dx}{dt} + \frac{cc-bb}{aa}yz = \frac{2gP}{Maa}$$

或等价地写成微分形式：
$$dx + \frac{cc-bb}{aa}yz\,dt = \frac{2gP\,dt}{Maa}$$

所有角度从方程中消失，整个刚体运动理论被包含在**三个简单的公式**中：

$$\boxed{dx + \frac{cc-bb}{aa}yz\,dt = \frac{2gP\,dt}{Maa}}$$

$$\boxed{dy + \frac{aa-cc}{bb}zx\,dt = \frac{2gQ\,dt}{Mbb}}$$

$$\boxed{dz + \frac{bb-aa}{cc}xy\,dt = \frac{2gR\,dt}{Mcc}}$$

这就是**欧拉方程**的最终形式——Ian Bruce称之为"Euler's Equations of Motion"。

> **批注**：这三个方程是欧拉在1765年给出的刚体动力学最一般的结果。用现代符号，令 $x = \omega_1$，$y = \omega_2$，$z = \omega_3$（角速度的三个主轴分量），$P$, $Q$, $R$ 为外力矩分量，$I_1 = Maa$，$I_2 = Mbb$，$I_3 = Mcc$ 为主转动惯量，则方程变为标准形式：
> $$I_1\dot{\omega}_1 + (I_3 - I_2)\omega_2\omega_3 = P$$
> $$I_2\dot{\omega}_2 + (I_1 - I_3)\omega_3\omega_1 = Q$$
> $$I_3\dot{\omega}_3 + (I_2 - I_1)\omega_1\omega_2 = R$$
>
> 欧拉自己也在注解807中指出，在前面第9章中通过"曲折路径"艰难推导出的结果，在这里从基本原理出发变得"最为透明"。这种用新方法重新推导旧结果并发现其简明性的做法，是欧拉科学方法的显著特征。

## 5. 自由旋转的守恒量（推论805）

当外力为零（$P = Q = R = 0$）时，欧拉方程变为：

$$dx + \frac{cc-bb}{aa}yz\,dt = 0$$
$$dy + \frac{aa-cc}{bb}zx\,dt = 0$$
$$dz + \frac{bb-aa}{cc}xy\,dt = 0$$

由此可以推出两个守恒量：

**能量守恒**（vis viva conservation）：

**推导**：将三个方程分别乘以 $aax$、$bby$、$ccz$，然后相加：
$$aax\,dx + bby\,dy + ccz\,dz + (cc-bb)xyz\,dt + (aa-cc)xyz\,dt + (bb-aa)xyz\,dt = 0$$

后三项之和为 $xyz\,dt(cc-bb+aa-cc+bb-aa) = 0$。因此：
$$aax\,dx + bby\,dy + ccz\,dz = 0$$

这可以写成 $d\left(\frac{1}{2}aax^2 + \frac{1}{2}bby^2 + \frac{1}{2}ccz^2\right) = 0$，积分得：
$$aaxx + bbyy + cczz = \text{Const.}$$

这正是动能守恒（乘以 $2/M$）：$T = \frac{1}{2}(I_1\omega_1^2 + I_2\omega_2^2 + I_3\omega_3^2) = \text{const.}$

**角动量守恒**（从消去 $dt$ 后积分得到）：

**推导**：从第一个方程 $dx = -\frac{cc-bb}{aa}yz\,dt$，和第二个方程 $dy = -\frac{aa-cc}{bb}zx\,dt$，消去 $dt$：
$$\frac{dx}{-\frac{cc-bb}{aa}yz} = \frac{dy}{-\frac{aa-cc}{bb}zx}$$

交叉相乘：
$$aa\,bb\,x\,dx = \frac{(cc-bb)(aa-cc)}{?}...$$

更直接的方法是将三个方程分别乘以 $aa\,dx$、$bb\,dy$、$cc\,dz$，利用守恒量1消去 $dt$。最终得到角动量分量之间的约束关系：
$$\frac{aa}{cc-bb}xx = \frac{bb}{aa-cc}yy + \text{Const.}$$

> **批注**：这两个守恒量分别对应于现代力学中的动能守恒和角动量分量之间的约束关系。欧拉在注解808中完整地解出了自由旋转问题——将 $y$ 和 $z$ 表示为 $x$ 的函数，然后通过对 $dt$ 的积分确定 $x$ 随时间的变化。这个解法虽然不如后来的雅可比（Jacobi）椭圆函数解法优美，但它是最早的系统解法。

## 6. 空间中主轴位置的确定（问题89-90）

问题89解决了：已知旋转轴相对于主轴的位置和角速度，如何确定主轴在绝对空间中的位置？

欧拉在以质心为中心的球面上建立一个固定的参考圆和参考点 Z，然后通过一系列微分方程描述主轴在球面上的运动。

问题90将所有结果汇总为**一般问题**的完整解答：给定初始运动和任意外力，确定刚体的运动。

最终的方程组包括：
- 三个欧拉方程（动力学方程）
- 描述主轴在空间中运动的辅助方程

> **批注**：问题90是欧拉刚体运动理论的终极总结。在推论810中，欧拉给出了完整的方程组——将欧拉方程与描述空间取向变化的方程结合。这组方程可以处理最一般的情况：任意形状的刚体、任意的初始条件、任意的外力。欧拉在注解814中指出，这些规则的应用范围远不止自由运动——无论刚体被约束在平面上、与其他物体碰撞、还是有一个固定点，都可以归约到这些规则。

## 7. 从基本原理到一般理论

欧拉在注解807中反思了自己的推导路径。他指出，前面第9章中通过"极其复杂的推理"才得到的结果——静止刚体受力后初始旋转轴的确定——在这里从基本原理出发"变得最为清晰"。

同样，前面无法触碰的一般受力运动问题，在这里"以同样的轻松和同样的努力顺利解决"。

欧拉自豪地总结道："仅仅在这一章中，从运动的基本原理出发，我们似乎就已经完整地建立了刚体运动的一般理论"。

> **批注**：欧拉的这番自我评价是客观的。在注解808中，他用变量替换 $x = \omega'\cos\alpha$，$y = \omega'\cos\beta$，$z = \omega'\cos\gamma$ 将所有角度从方程中消去，得到了极其简洁的三个方程——这就是欧拉方程的最终形式。这种从复杂的三角函数表达中提取出核心结构的能力，是欧拉数学天才的标志。从历史的角度看，这三个方程至今仍是航空航天、机器人学、天体力学和分子动力学等领域的基本方程。每当我们看到卫星调整姿态、陀螺仪保持平衡、或者分子动力学模拟蛋白质折叠，背后都有欧拉在1765年建立的这些方程在工作。

---

## 本章小结

正文第10章是全书的理论巅峰：

1. **运动的四要素**：质心速度、方向、角速度、旋转轴的变化
2. **角速度的分量表达**：速度沿主轴分解（叉积的首次出现）
3. **欧拉方程的推导**：从基本原理逐步构建
4. **简化形式**：$dx + \frac{cc-bb}{aa}yz\,dt = \frac{2gP\,dt}{Maa}$ 等
5. **守恒量**：自由旋转的能量守恒和角动量守恒
6. **空间取向**：主轴在绝对空间中位置的确定
7. **一般问题**：任意刚体、任意力、任意初始条件的完整解答

> **批注**：欧拉方程的建立是经典力学史上的里程碑。从牛顿（1687）到欧拉（1765），从质点到刚体，从平动到旋转，力学走过了近80年的历程。欧拉在注解814中展望了未来的应用方向：刚体碰撞、天体自转等。他特别提到了天体的旋转运动——这是后来被克莱罗（Clairaut）、达朗贝尔（d'Alembert）和欧拉自己在月球运动理论中深入研究的课题。从思想史的角度看，欧拉方程的建立标志着力学从"质点时代"进入"连续体时代"的关键转折。

---

[上一章：正文第9章——刚体运动初始生成的规律](欧拉刚体运动理论_正文_第9章.md)

[下一章：正文第11章——受任意力作用的刚体运动](欧拉刚体运动理论_正文_第11章.md)
